---
scope: Rez Ops v1 -- AI agent framework for Disaster Recovery program management
purpose: build substrate for implementation; user also wants a walkthrough deck and fuller solution-design doc produced at finalize
altitude: initiative (product-level spine, no epics/stories exist yet)
updated: 2026-09-12T16:33
---

- (event) Fast path chosen; source input is brief.md + addendum.md from brief-Resillience-Ops-2026-08-12; no UX artifact, no prior spine, greenfield
- (version) MCP Python SDK 'mcp' package: pin to stable v1.29.x (<2); v2.0.0 GA shipped 2026-07-28 with breaking changes (split packages, new handshake) and is too new (~2 weeks old) to build v1 on. Verified via web research 2026-08-12
- (version) Claude Code: project-scoped .mcp.json for shared connector registration (precedence local > project > user); headless invocation via 'claude -p --output-format json', with new --bare flag recommended for cron/CI determinism (will become -p default later). Verified via web research 2026-08-12
- (assumption) MCP momentum verified real, not stale training data: donated to Agentic AI Foundation (Linux Foundation) Dec 2025, 10k+ public servers, 97M+ monthly SDK downloads, client support across Claude/ChatGPT/Gemini/Copilot/Cursor/Codex CLI/OpenCode/Devin as of Nov 2025 anniversary post -- substantiates the 'connectors portable across any MCP-compatible runtime' bet in the brief
- (decision) AD-1 Paradigm: three-layer Connector / Ledger-Core / Runtime split (Sensors-Ledger-Voice). Binds: all. Prevents: freshness/confidence/ownership business logic leaking into connectors or into runtime-specific prompts/skills, breaking consistency and cross-runtime portability. Rule: connectors are dumb read-only fetch+normalize MCP servers; ledger-core is sole owner of the Freshness Ledger and all derived computation; the agent runtime (Claude Code or any MCP client) only orchestrates and presents, never holds domain logic
- (decision) AD-2 Server topology: one MCP server per data-domain connector (calendar, ticketing, git, CMDB) plus one separate ledger-core MCP server. Binds: all connector/core components. Prevents: a monolithic server accreting mixed responsibilities and bloating every session's tool/context budget (Claude Code docs explicitly warn connected-server tool lists load into every session context). Rule: new domains = new connector server, never extend an existing server's scope
- (decision) AD-3 State mutation: append-only event log with materialized projection. Binds: ledger-core. Prevents: silent unaudited edits to freshness/ownership state that destroy the history the trust-layer non-negotiable requires. Rule: all state changes are appended as immutable timestamped events to a git-committed per-artifact-type log; current state is always a pure recomputed projection over the log, never hand-edited in place
- (decision) AD-4 Shared ledger schema ownership. Binds: connectors + ledger-core. Prevents: each connector inventing its own record shape for last-verified/owner/tier/etc, producing data ledger-core cannot merge. Rule: one shared, versioned schema module defines every artifact-type record shape; every connector and ledger-core import it rather than declaring ad hoc fields
- (decision) AD-5 Confidence/coverage computation ownership. Binds: ledger-core only. Prevents: connectors independently guessing confidence scores with inconsistent methodology, undermining the never-hide-uncertainty non-negotiable. Rule: confidence/coverage values are computed exclusively by ledger-core from raw connector facts using one documented method; connectors report only raw timestamps/records, never a confidence score
- (decision) AD-6 Draft-not-send output boundary. Binds: ledger-core, any future write-capable connector. Prevents: an accidental or later-added write-back path bypassing the human-approval non-negotiable from the brief. Rule: any agent-authored outbound content is written only to a git-tracked drafts/ queue as a pending record; no component calls an external send/write API directly in v1
- (decision) AD-7 Runtime portability / operational envelope. Binds: all. Prevents: assuming hosted infra that would lock Rez Ops to one runtime or require more than a laptop+git. Rule: v1 is local-first -- no hosted DB, no container orchestration, no persistent server process; durability comes from a git remote pushed after each session; scheduled work is OS-scheduler-triggered (cron/launchd) headless runtime invocation; any MCP-client-capable coding-agent runtime can host the same servers unmodified
- (decision) Consistency convention -- naming: artifact-type records keyed by domain (bia, tiering, runbooks, raci, test_records); ledger files at ledger_data/{artifact-type}/{artifact-id}.yaml; append-only event logs at ledger_data/{artifact-type}.log.md, memlog-style (mirrors this project's own memlog.py pattern)
- (decision) Consistency convention -- data & formats: dates ISO 8601 UTC; every record carries last_verified, verification_method, expiry_rule, tier_sla, escalation_owner, confidence (agent-verified|manual|unknown) per the shared schema (AD-4); IDs are stable slugs, never reused
- (decision) Consistency convention -- state & cross-cutting: all mutation goes through ledger-core's single append-only writer (AD-3); every write records actor+timestamp+reason; unverifiable data escalates to an explicit unknown/unverified confidence state, never silently dropped; connector/config lives in .mcp.json + rezops.config.yaml, no secrets committed to git
- (assumption) Seed/stack: Python 3.12+ for all MCP servers (connectors + ledger-core), FastMCP-style decorator ergonomics, familiar for ops/DevOps scripting, fast to stand up many small servers -- ASSUMPTION, ok on Fast path, easy to swap per-connector later since AD-2 keeps servers independent
- (assumption) Seed/stack: git (local repo + private remote e.g. GitHub) is the only persistence layer -- no database server. Storage medium chosen for AD-7 (local-first, self-recoverable) and AD-3 (append-only log fits git history naturally)
- (direction) Deferred: delivery channel for the daily/periodic briefing (Slack/email/terminal) -- brief already scopes this as swappable, decide on first real usage
- (direction) Deferred: write-back/auto-actioning, Blast Radius Rewind scoreboard, proactive micro-drills -- explicitly out of v1 scope per brief
- (direction) Deferred: packaging (personal config vs installable product for other DR practitioners) -- explicitly left open in the brief itself
- (direction) Deferred: specific vendor adapters (which calendar/ticketing/CMDB product Olu actually uses) -- not yet specified; AD-2/AD-4 make the connector interface vendor-agnostic so a concrete adapter can be added later without redesign
- (direction) Deferred: ledger-core's exact confidence-scoring formula -- AD-5 fixes ownership (who computes it), not the method itself, which is implementation detail owned by the code once written
- (direction) Deferred: MCP SDK v2 migration -- v2 shipped 2026-07-28, too new to commit to; revisit once spec/SDK settle
- (direction) Deferred: multi-user/multi-tenant support -- v1 is single-owner (Olu) only, per the brief's stated primary user
- (event) reviewer gate ran: lint_spine.py (3 low false-positive placeholder hits on intentional {artifact-type}/{artifact-id} path-pattern tokens, no action), reconcile-vs-brief, rubric walker, version-currency lens, adversarial lens. Findings triaged below; reviews/ subfolder holds full reports
- (decision) BLOCKER FIX -- AD-7 headless command corrected: version-currency reviewer found claude -p --bare skips MCP-server autodiscovery entirely, meaning the documented cron command would run the daily briefing with zero connectors/ledger-core attached. Corrected to 'claude -p --mcp-config .mcp.json --output-format json' (drop --bare since Rez Ops fundamentally depends on MCP servers); determinism trade-off (hooks/CLAUDE.md still load) accepted for now, noted as a later optimization
- (decision) BLOCKER FIX -- Stack table: bumped Python floor from 3.12+ to 3.13+ (3.12 already in security-only maintenance per version-currency reviewer); Claude Code entry changed from vague 'current' to a capability floor (must support project-scoped .mcp.json + -p --mcp-config --output-format json headless mode), checkable via 'claude --version', since an exact pinned number goes stale fast for a fast-moving CLI
- (decision) AD-8 (new) Graceful degradation -- the brief's non-negotiable was missing entirely from the spine. Binds: all. Prevents: Rez Ops ever blocking, replacing, or degrading below today's manual DR-program baseline if it is wrong, down, or a connector fails. Rule: every runtime-facing operation must fail open -- a connector or ledger-core outage surfaces as an explicit unknown/unverified state (never a crash that blocks the human), and no AD or component may require Rez Ops to be running for the underlying DR program to function
- (decision) AD-9 (new) Shared schema split: RawFact vs LedgerRecord -- adversarial reviewer found connectors and ledger-core sharing one undifferentiated schema shape caused confidence-field ambiguity, tier_sla source conflicts, and missing provenance. Also closes the reconcile-gap finding that the brief's 'every inferred claim shows its sources' trust-layer requirement was unaddressed. Binds: connectors, ledger-core, shared schema module. Prevents: connectors asserting computed/owned fields (confidence, tier_sla) that only ledger-core may set, and losing the source-provenance trail required for compliance/CISO adoption. Rule: the shared schema module defines two distinct shapes -- RawFact (connector-writable: raw observed data + a source reference back to the origin system/record) and LedgerRecord (ledger-core-only: last_verified, verification_method, expiry_rule, tier_sla, escalation_owner, confidence -- all computed, never connector-supplied). tier_sla specifically is ledger-core-computed from rezops.config.yaml policy inputs; a connector reporting tier_sla is a schema violation
- (decision) AD-10 (new) Ownership arbitration and inference -- promotes the brief's stated wedge feature (ownership inferred from live activity, orphan-risk list) to its own AD, and closes the adversarial finding that two connectors reporting contradictory escalation_owner values for the same entity had no arbitration rule (last-write-wins was the implicit, wrong default). Binds: ledger-core. Prevents: silent last-write-wins ownership conflicts, and the ownership wedge feature staying unimplemented as vague prose. Rule: ledger-core assigns exactly one authoritative connector per ownership-bearing field per entity (a per-field ownership map, e.g. HRIS/org-chart connector owns escalation_owner over ticketing); a conflicting raw fact from a non-authoritative source is logged but never overwrites the authoritative value; total absence of any authoritative signal escalates the field to confidence=unknown and adds the entity to the orphan-risk list, rather than guessing
- (decision) Amend AD-2 in place: added tool-naming convention -- adversarial reviewer found two connectors independently naming identically-shaped tools (list_items/get_item) would collide in the runtime's tool namespace despite both obeying AD-2's server-per-domain rule. New Rule clause: every tool name is domain-prefixed (e.g. calendar_list_items, cmdb_get_item) and each server's MCP registration key is fixed to its domain slug
- (decision) Amend AD-6 in place: added enforcement mechanism -- adversarial reviewer found AD-6's literal text (drafts written to ledger_data/drafts/) allowed a future write-capable connector to write there directly, creating two writers to one directory, contradicting AD-3's single-writer rule. New Rule clause: drafts are created only by calling ledger-core's write tool, never by direct filesystem writes from any other component
- (decision) Amend AD-1 in place: added enforcement note -- 'no domain logic in the runtime' had no enforcement mechanism (rubric walker finding). Clarified as a code-review convention: domain logic (freshness rules, confidence computation, ownership inference) may only live inside a connector or ledger-core tool implementation, never in a skill/prompt/system-instruction file, since prompt-embedded logic cannot be enforced by tooling and must be caught in review
- (decision) Amend AD-7 in place: added failure/alerting and credential rules -- rubric walker found no failure contract for the scheduled headless run and no credential mechanism for connectors (only a 'no secrets in git' constraint, not a mechanism). New Rule clauses: (1) a failed scheduled run appends an error entry to ledger_data/_ops.log.md rather than failing silently; (2) each connector's credential comes from the OS keychain or a per-connector env var named REZOPS_{DOMAIN}_TOKEN, never from rezops.config.yaml or any git-tracked file
- (direction) Deferred (added, was missing): favor fewer high-trust provenance-ranked sources over breadth for v1 -- explicit brief out-of-scope item, omitted from first spine draft
- (direction) Deferred (added, was missing): passive-observation watch-only baselining period -- explicit brief out-of-scope item, omitted from first spine draft
- (direction) Deferred (added): git remote push/conflict handling strategy -- rubric walker flagged this as silent; genuinely fine to defer for a single-owner v1, revisit once multi-session/multi-device usage actually produces a conflict
- (event) re-lint clean except 3 low false-positive placeholder hits on intentional {artifact-type}/{artifact-id} path-pattern tokens (ledger_data/{artifact-type}/{artifact-id}.yaml naming convention) -- reviewed and ignored, not unfilled template vars
- (event) spine finalized: 10 ADs (7 original + AD-8 graceful degradation, AD-9 schema split, AD-10 ownership arbitration), AD-1/2/6/7 amended in place per reviewer gate, Deferred list completed against brief's explicit out-of-scope items
- (event) renderings produced: solution-design.md (fuller rationale doc) and walkthrough-deck.html (10-slide interactive HTML+SVG deck), per user's explicit request for both. bmad-review structure+prose polish applied to solution-design.md only (moved Structural reference section earlier, condensed Reading-this-alongside-the-brief to a pointer map, plus wording/terminology-consistency fixes); deck reviewed by its own author against the brief spec and needed no changes. Spine itself untouched by doc-standards polish, per skill rule
- (event) no external_handoffs configured -- nothing to execute. Run complete: ARCHITECTURE-SPINE.md (status: final), solution-design.md, walkthrough-deck.html, .memlog.md all in place
- (event) Update run started on branch feature/policy-and-evidence: adding a governed decision-and-action layer (EvidenceBundle, ActionProposal, Policy Engine) per converged research + conversation with Olu. Fast path: drafting with [ASSUMPTION] tags for review. All 10 existing ADs and non-negotiables preserved as invariants -- extension, not rewrite.
- (decision) AD-11 (new) Evidence boundary. Binds: shared schema module, ledger-core, Voice. Prevents: a reasoning-layer claim (e.g. 'this runbook looks stale') existing only as prose with no attributable source, undermining never-hide-uncertainty at the reasoning layer the same way an unattributed RawFact would at the data layer. Rule: a new shared schema type EvidenceBundle (claim, confidence: float 0-1, evidence: list of EvidenceRef, reasoning, generated_at) where each EvidenceRef points back to an already-ingested RawFact's source or a LedgerRecord field -- never inline-duplicates fact content, only cites it. EvidenceBundle.confidence is a claim-level plausibility score distinct from LedgerRecord.confidence's agent-verified/manual/unknown enum (AD-5) -- the two are never conflated or substituted for each other.
- (decision) AD-11 continued: EvidenceBundle authorship vs domain logic boundary [ASSUMPTION]. Voice assembles the claim/reasoning text (this is reasoning, not domain-logic computation -- AD-1's prohibition covers freshness/confidence/ownership computation, not structuring a citation over facts ledger-core already computed). But persistence goes through a new ledger-core write tool only (mirrors AD-6's drafts mechanism exactly: Voice calls it, ledger-core is the sole writer) -- Voice never writes ledger_data/ directly. Stored one-file-per-bundle at ledger_data/evidence/{evidence_id}.md, same human-readable/git-diffable convention as drafts.
- (decision) AD-12 (new) ActionProposal + Policy Engine. Binds: ledger-core, shared schema module, Voice. Prevents: (a) conflating drafted human-readable outbound content (AD-6's Draft, unchanged) with a structured, policy-evaluated system operation -- these need different schemas and different eventual consumers; (b) the LLM ever deciding for itself whether an action is permitted, which is the one property this whole layer exists to guarantee. Rule: ActionProposal (action, target: artifact_type/artifact_id, reason, evidence: list of EvidenceBundle refs, impact, policy_decision) is created by Voice calling a new ledger-core tool naming a fixed action-vocabulary identifier (never freeform) and citing at least one EvidenceBundle -- a proposal with zero evidence is invalid, rejected the same way empty Draft fields already are. Ledger-core -- never Voice -- computes policy_decision (automatic | requires_approval | denied) by resolving the named action's config-declared risk/impact against the target's criticality (from LedgerRecord/config) and the cited evidence's confidence, exactly extending AD-5's existing 'ledger-core exclusively computes derived values' principle rather than inventing a new exception to it.
- (decision) AD-12 continued: Never clause -- no executor exists in this phase. Rule: policy_decision is recorded, never acted on -- even an 'automatic' decision triggers no external write; nothing in this phase calls an external write/send API (extends AD-6's existing prohibition to the new object). Building an Executor that actually performs an approved action is an explicit, separate, later architectural decision requiring its own AD before any code writes externally -- out of scope here. Stored one-file-per-proposal at ledger_data/action_proposals/{proposal_id}.md, mirroring drafts'/evidence's convention.
- (decision) AD-12 continued: policy config location [ASSUMPTION]. Per-action risk/impact classification and required-approval rules live in a new git-tracked rezops.policy.yaml, kept separate from the existing rezops.config.yaml (connector/tiering policy inputs per AD-9) rather than merged into it -- mirrors AD-2's existing lesson that one config/server accreting mixed responsibilities causes drift; an authorization policy table and tiering/connector config are different concerns with different owners and review cadence.
- (decision) Amend AD-1 in place: reframe the read-only non-negotiable. Old implicit rule (connectors are read-only, AD-6 forbids direct external writes) restated explicitly as its own clause: 'Read-only by default; action is an explicit, separately-gated capability' -- the invariant that made the v1 architecture trustworthy is preserved exactly, not weakened, by naming ActionProposal+Policy Engine (AD-12) as the one named, opt-in door through which 'action' as a concept is even allowed to exist, with no executor behind that door yet.
- (direction) Deferred (added): the Executor -- actually performing a policy-approved action against an external system. Explicitly out of scope for this amendment; requires its own future AD once the team deliberately decides to cross that line, per Olu's own research conclusion (get the propose/evidence/policy loop working well first).
- (direction) Deferred (added, was not an architectural decision): specialized reasoning framing (risk/compliance/resilience/evidence 'agents') is Voice-side prompt/persona engineering on the existing single Voice, never a new code-level component -- fails the spine's own inclusion test (not an independently-built unit that could diverge), so it gets no AD; noted here only so it isn't silently re-litigated later as if it were still open.
- (event) Reviewer gate ran on the amended spine: lint_spine.py (7 low false-positive placeholder hits on {evidence_id}/{proposal_id}/{artifact-type}/{artifact-id} path-pattern tokens, same class already accepted for the original spine -- no action), rubric walker, version-currency (clean pass, no new dependencies), adversarial (first attempt failed on an unrelated API/sleep error mid-run, retried clean under review-adversarial-new-ads.md so as not to overwrite the original spine's review-adversarial.md).
- (decision) BLOCKER FIX -- AD-11: EvidenceBundle.confidence ownership was unstated; both the rubric walker and the adversarial reviewer independently found this let Voice self-supply the confidence value AD-12's policy evaluation reads, reopening the exact self-approval loophole AD-12 exists to prevent. Fixed: ledger-core -- never Voice -- computes confidence server-side from the cited evidence; a caller-supplied value is now an explicit schema violation, mirroring AD-9's tier_sla precedent.
- (decision) BLOCKER FIX -- AD-11: EvidenceRef had no enumerated shape (adversarial reviewer: prose-only citation was ambiguous between a bare string and a structured object). Fixed: EvidenceRef is now a structured {source, field} object -- source mirrors RawFact.source's format, field (optional) names the specific LedgerRecord field being cited when the evidence is computed state rather than a raw fact.
- (decision) BLOCKER FIX -- AD-12: the 'fixed vocabulary of action identifiers' had no canonical source (both reviewers flagged this independently). Fixed: the vocabulary is now defined as exactly the top-level keys of rezops.policy.yaml -- single source of truth, discoverable by Voice by reading the same file ledger-core evaluates against.
- (decision) BLOCKER FIX -- AD-12: policy_decision's resolution had no documented-method requirement and no confidence-aggregation rule across a multi-item evidence list (rubric walker + adversarial reviewer both flagged, independently, from different angles). Fixed: added AD-5-style 'one documented method' clause; fixed the aggregation rule to minimum confidence across cited EvidenceBundles (most cautious evidence sets the ceiling, never an average); a target with no LedgerRecord/tier_sla yet resolves to the most conservative criticality tier rather than a guess, mirroring AD-10's escalate-not-guess precedent.
- (decision) BLOCKER FIX -- AD-12: ActionProposal's storage model changed from one-file-per-proposal (mirroring Draft) to a single append-only log (ledger_data/action_proposals.log.md) with proposed/decided events and state as a pure projection, extending AD-3's discipline. Root cause (adversarial reviewer): unlike Draft/EvidenceBundle, an ActionProposal has a real lifecycle (proposed then decided) with no specified recording mechanism for the decision that didn't risk either an in-place mutation (violating AD-3) or an unlisted new log two implementers could invent differently. Also tightened the Never clause from temporal ('no executor exists in this phase') to structural ('nothing may act on policy_decision regardless of value, including an internally-triggered Draft write') per the adversarial reviewer's finding that the temporal wording didn't foreclose an automatic decision quietly triggering AD-6's Draft path.
- (decision) Amend AD-12's Prevents/Rule for the caller-supplied policy_decision guard, mirroring AD-9's tier_sla precision exactly, per rubric walker's low-severity finding (fixed alongside the higher-severity ones above rather than deferred, since it was a one-clause addition already implied by the ledger-core-only-computes-it language).
- (event) All five real findings from the reviewer gate (2 blocker-severity converged across both reviewers, 3 high-severity from the adversarial pass) fixed in place. Stack-table currency (rubric walker's one low finding) needed no change -- version-currency reviewer already confirmed no new dependency was introduced by this pass.
- (event) Renderings updated per Olu's chosen deliverable scope (spine + both companion docs): solution-design.md gets a new 'Extending the paradigm' section narrating AD-11/AD-12 and the review findings that shaped them, plus updated structural-reference tree, deferred list, and brief-mapping caveat; walkthrough-deck.html gets a new 'Evidence & Policy' slide (reusing existing decision-card/callout components, no new CSS), an updated Deferred slide (Executor replaces the now-partially-resolved write-back item), and an updated Closing slide's read-only-first row. Slide count/nav is derived dynamically from .slide elements in the deck's own JS, so no hardcoded count needed updating.
- (event) Spine amendment complete: 12 ADs total (10 original + AD-11 Evidence boundary + AD-12 ActionProposal/Policy Engine), AD-1/AD-6 amended in place, reviewer gate re-run clean after fixes (lint: 5 low false-positives, same accepted class as before; version-currency: pass, no new deps; rubric walker + adversarial: both real findings fixed in place). Run complete on branch feature/policy-and-evidence: ARCHITECTURE-SPINE.md, solution-design.md, walkthrough-deck.html, .memlog.md all updated; status remains 'final'.
- (event) Doc-standards polish applied to solution-design.md's new content only (structure+prose lenses, bmad-review), per the skill's own Finalize step 5 which was skipped in the prior pass. Finding: the new 'Extending the paradigm' section was 952 words -- the longest section in the document despite covering only 2 ADs, denser than 'Schema and ownership' (749 words, 4 ADs) -- caused by repeated framing across paragraphs ('the adversarial reviewer found... independently...') and a standalone closing paragraph that could merge into its parent. Condensed to 701 words (-26%) preserving every substantive finding; no other new content needed changes. ARCHITECTURE-SPINE.md itself was untouched by this pass, per the rule that doc-standards polish never applies to the spine.
- (event) spine finalized (amendment). ARCHITECTURE-SPINE.md: status remains 'final', updated 2026-09-05, 12 ADs (10 original + AD-11, AD-12), AD-1/AD-6 amended in place. Reviewer gate clean after fixes. Companion docs (solution-design.md, walkthrough-deck.html) updated and polished to match. Run complete on branch feature/policy-and-evidence.
- (event) Post-implementation correction (Story 12 review, blind-hunter finding): AD-11's literal text still said 'the shared schema module adds EvidenceBundle' after Story 12 deliberately implemented it in ledger_core/ instead (documented in the story's own Design Notes, but the spine itself was never reconciled -- a documentation-fidelity gap). Amended AD-11's Rule text, the Consistency Conventions data-formats row, the Structural Seed tree comment, and the Invariants diagram to reflect the actual module placement and EvidenceRef's real shape (artifact_type/artifact_id added alongside source/field -- also resolves an ambiguity the original terse wording left open, which Story 12's review caught as an actual resolution bug when the artifact scoping wasn't correctly enforced in code).
- (decision) Executor design session started (design-only, per AD-12's Never clause requiring its own deliberate AD before any external write is built -- no execution code lands in this pass). Trigger mechanism: human-explicit-invoke only -- a human (or Voice, on explicit human instruction) names one specific proposal_id to execute. Nothing polls for or auto-fires automatic/approved proposals in this design. Confirmed by Olu over human-invoke vs. a polled/scheduled auto-executor alternative -- keeps the first Executor version fully human-paced, matching the project's smoke-detector posture; autonomous polled execution would be a deliberate later step, not bundled here.
- (decision) Executor component shape: a new category, 'Executors' (executors/), mirroring Sensors -- one small, domain/action-scoped MCP server per action type/vendor, write-capable siblings to the existing read-only Sensors. Each executor re-fetches its target ActionProposal from ledger-core, confirms policy_decision is favorable, performs the real external write, then calls a new ledger-core tool to record the outcome. Ledger-core itself gains no per-vendor write code and remains the sole writer to ledger_data/ (AD-3 preserved). Confirmed by Olu over two alternatives: a new tool directly on ledger-core (rejected -- would pull vendor-specific write code into the one server meant to stay pure computation, the exact anti-pattern AD-2 already forbids for Sensors) and a standalone script (rejected -- bypasses the MCP-tool-boundary discipline everything else in this project follows).
- (decision) Execution recording: a NEW, separate append-only log, ledger_data/action_executions.log.md, tracking zero-or-more execution ATTEMPTS per proposal_id (each carrying outcome success/failure, timestamp, executor identity, an external reference e.g. the created ticket's ID) -- NOT a third event type on action_proposals.log.md. Confirmed by Olu over extending the existing proposed/decided log directly (rejected -- that log's model assumes exactly one of each event type per proposal_id, already enforced/tested in Story 13 as a corruption signal if violated; execution can genuinely retry after a transient failure, which doesn't fit that one-shot model). Idempotency: reject re-executing a proposal_id that already has a SUCCESSFUL execution attempt; allow retrying one whose only attempts so far failed.
- (decision) Approval step: a requires_approval ActionProposal needs its own explicit, distinct approved event (who, when) via a new ledger-core tool (e.g. ledger_approve_action_proposal(proposal_id, approver)) before any Executor will accept it for execution -- confirmed by Olu over 'human-invokes-execute already is the approval' (rejected -- collapses two potentially-different roles/people, who approved vs who executed, losing the audit distinction the whole evidence/policy layer exists to provide). approved fits as a THIRD event type on the EXISTING action_proposals.log.md (alongside proposed/decided) -- like decided, it's a one-time state change per proposal, not a repeatable attempt, so it belongs with those two, not in the new action_executions.log.md.
- (decision) Executor safety constraints, confirmed: (1) denied proposals are hard-blocked at the Executor, no override, regardless of who invokes it. (2) automatic proposals need no approval event but still require deliberate human invocation to execute (trigger mechanism decision already established this). (3) No automatic rollback/undo of any kind -- reversing an executed action, if ever needed, is a manual human action entirely outside this system, the same category as AD-6's send-a-drafted-message-is-manual precedent.
- (event) Reviewer gate ran on AD-13/14/15: version-currency PASS (no new dependency); rubric walker CONDITIONAL PASS (2 major findings: the whole approval/denial/idempotency safety chain was specified as per-Executor-implemented with no centralized ledger-core gate, contradicting the established pattern that ledger-core alone computes derived/authorization values; action_proposals.log.md's approved line in Structural Seed was missing its design-only tag); adversarial found 4 CRITICAL + 1 HIGH: Voice could self-supply a fabricated approver identity (defeats AD-14's whole purpose); 'explicit human instruction' triggering execution has no actual enforcement mechanism distinguishing it from Voice acting on its own initiative; the idempotency check-then-act has a race window; the Executor re-fetch mechanism AD-13 assumed doesn't exist and would force duplicating ledger-core's projection logic outside ledger_core/; no exclusive-ownership rule prevents two Executor servers both claiming one action name.
- (decision) BLOCKER FIX -- AD-13 substantially revised: introduces one centralized ledger-core tool, ledger_authorize_execution(proposal_id), as the SOLE sanctioned way an Executor may address a proposal by id and the SOLE place the approval/denial/idempotency safety chain is checked -- closing both the rubric walker's per-Executor-safety-chain finding and the adversarial reviewer's missing-re-fetch-mechanism finding at once, and matching this project's established pattern (AD-1/AD-5/AD-9/AD-11/AD-12) that ledger-core alone computes/authorizes derived values, never a caller. Authorization itself appends an 'attempted' marker to action_executions.log.md (AD-15) at authorization time, not after the real write -- narrowing (not claimed to eliminate) the race window between two concurrent invocations, which remains a named, called-out Deferred risk rather than a silently-inherited one given the higher real-world stakes of a double-executed write vs. a merely-interleaved log line.
- (decision) BLOCKER FIX -- AD-13: execution is only ever invoked following a real MCP client-side interactive confirmation, using MCP's own elicitation primitive, at the point of execution -- not merely because Voice chose to call a tool. Closes the adversarial reviewer's 'explicit human instruction is unenforceable' finding using an actual MCP protocol mechanism (confirmed to exist in the pinned MCP spec version by the version-currency reviewer) rather than a hand-rolled, unenforceable convention. This is the one point in the whole system where a real external, potentially-irreversible consequence occurs -- every earlier write tool (Draft, EvidenceBundle, ActionProposal creation) stays low-stakes/reversible/inspectable and doesn't need this, which is why the gap only became load-bearing here.
- (decision) BLOCKER FIX -- AD-14 revised: approved's approver field is never a freeform argument Voice supplies -- it resolves from a fixed, non-Voice-suppliable operator identity (e.g. a REZOPS_OPERATOR_NAME env var set once by the human operating this installation), the same never-from-an-LLM-suppliable-parameter discipline AD-7 already established for credentials. Closes the adversarial reviewer's Voice-can-forge-approval-provenance finding. Rez Ops v1 has exactly one operator (SPEC.md's existing multi-user Non-goal already establishes this), so 'who approved this' can only honestly mean 'the person running this session' -- not a cryptographically-verified third party, which would need a real identity/auth system explicitly out of scope for v1. Also: a second approved event for an already-approved proposal_id is rejected as corruption, mirroring AD-12/Story 13's existing duplicate-decided-event precedent (closes the adversarial reviewer's medium-severity duplicate-approved-event finding).
- (decision) Fix -- AD-13: rezops.policy.yaml declares exactly one authoritative executor per action (a new per-action field, e.g. executor: ticketing), extending the same per-field-single-owner principle AD-10 already established for ownership arbitration to the write side. Two Executor servers claiming the same action is now a declared config error, not a runtime race -- closes the adversarial reviewer's high-severity no-exclusive-ownership finding.
- (decision) Fix -- AD-13's 'favorable' policy_decision gloss now defined explicitly and precisely: automatic, or requires_approval with a matching approved event and no denied status -- never left as an undefined term two implementers could read differently (rubric walker minor finding).
- (event) Consistency Conventions table's stale 'no executor consumes policy_decision (AD-12)' line (predating this session) revised to reflect AD-13/14/15's design without contradicting it; Structural Seed's action_proposals.log.md line now correctly tags the AD-14 approved event as design-only, matching the sibling executors/ and action_executions.log.md lines (both rubric walker findings).
- (event) All reviewer-gate findings from the AD-13/14/15 pass applied and cross-checked: lint clean (same 5 accepted false positives as before), all 4 CRITICAL + 1 HIGH adversarial findings and both rubric-walker Major findings addressed by the AD-13/14/15 rewrite (centralized ledger_authorize_execution gate, operator-attested approval identity, MCP elicitation for human confirmation, named residual race-window risk, per-action executor ownership in rezops.policy.yaml, corrected Structural Seed/Consistency Conventions staleness). 15 ADs total now (12 built, AD-13/14/15 design-only). Moving to solution-design.md next.
- (event) solution-design.md updated to match: new 'Designing the Executor, before building it (AD-13, AD-14, AD-15)' section narrating the coaching session and the reviewer-gate findings (condensed once, 1165->814 words, matching the AD-11/12 section's own proportions); Deferred section's stale 'The Executor' bullet updated to reflect that the AD now exists (design, not build); new deferred bullets for autonomous/polled execution, the undecided 'rejected' event, and the named residual race-window risk; closing brief-mapping section extended to AD-15. Also fixed, incidentally: the Structural reference tree near the top of the document had never actually been updated during the earlier AD-11/12 pass (only ARCHITECTURE-SPINE.md's own tree was) -- now corrected to match, plus the new AD-13/14/15 additions.
- (event) spine finalized (AD-13/14/15 design session). ARCHITECTURE-SPINE.md: 15 ADs total (12 built, AD-13/14/15 explicitly tagged [DESIGN ONLY -- not yet built]), status remains 'final', reviewer gate clean after fixes. solution-design.md updated to match. No code touched anywhere in this session -- by design. Run complete on branch main (this design session happened directly on main, not a feature branch, since nothing here is buildable/testable code).
- (event) Update run started on branch feat/rez-ops-next-generation (2026-09-12): Phase 0 architecture baseline for the Product Direction & Evolution Plan (docs/product-direction.md). Coaching path chosen by Olu. Deliverables: amended spine + docs/architecture-next.md + docs/provider-adapter-contract.md (Phase 0 docs, audience = coding agents/implementers) + updated solution-design.md and walkthrough-deck.html. Baseline verified: 820 tests pass, no production code changes in this run. All 15 existing ADs and non-negotiables preserved as invariants -- extension, not rewrite.
- (constraint) Inherited silently from docs/product-direction.md (already settled by Olu, not re-asked): Sensors contract unchanged (sec 5); Ledger never depends on an LLM for deterministic state (sec 6); policy is config, never prompts (sec 34); local-first stays default, hosted is a deliberate later phase (sec 39); append-only persistence stays, no premature DB migration (sec 40); routing must be deterministic and inspectable, never an LLM choosing the model (sec 22/48); provider adapters launch the vendor's official local runtime and never extract/proxy/convert credentials (sec 19/20); no AI provider may become a system of record (sec 87); no wholesale repo reorganisation, move files only for a concrete architectural reason (sec 78); Phase 1 builds no routing, no multi-agent, no Ledger changes (sec 63).
- (decision) AD-16 (new) Two execution modes, one MCP surface. Confirmed by Olu over full inversion (rejected: rewrites the usage model every story assumed; contradicts plan sec 79 Rule 2 preserve existing contracts) and Ledger-orchestrates-agents (rejected: Ledger would depend on an LLM, forbidden by AD-1 and plan sec 6). Binds: agent runtime, Voice, ledger-core, connectors. Prevents: two paths into Rez Ops diverging on which tools exist or how claims enter the Ledger; agent-runtime concerns leaking into ledger_core/. Rule: HOSTED mode = a human's MCP client loads Rez Ops servers from .mcp.json (today, unchanged). DRIVEN mode = the agent runtime (new top-level agents/ package, a peer of ops/, never inside ledger_core/) launches a provider's official local CLI headless and points it at the same .mcp.json. Both modes reach the Ledger only through the same MCP tools; neither carries domain logic. AD-7's literal 'claude -p --mcp-config' clause becomes the claude adapter's implementation detail, not the architecture.
- (decision) AD-17 (new) Provider capabilities are probed, never declared. Confirmed by Olu over a declared rezops.providers.yaml capability matrix (rejected: plan sec 21 itself says do not hard-code without an integration test; a stale declaration silently routes work to a runtime that cannot do it) and a declared-then-verified hybrid (rejected: more machinery for the same outcome). Binds: every provider adapter, rezops doctor, future routing. Prevents: two adapters reporting availability by different methods; config asserting a capability the runtime no longer has. Rule: an adapter's health/capability report (installed, authenticated, healthy, MCP, headless, sessions, structured output, auth mode SUBSCRIPTION|API) comes only from probing the actual runtime at call time. Config (a future rezops.providers.yaml) holds routing PREFERENCES only, never capability claims. rezops doctor is the human-facing surface of the same probe. Extends AD-7's verify-at-build-time discipline to run time.
- (decision) AD-18 (new) Agent state lives in its own append-only store, agent_data/, owned by the agent runtime. Confirmed by Olu over new logs inside ledger_data/ written by ledger-core (rejected: pulls agent-runtime concepts into the Ledger schema and makes ledger-core the writer of state it never derives, the blurring plan sec 28 argues against) and SQLite now (rejected: plan sec 40 says do not migrate prematurely -- Deferred). Binds: agent runtime, ledger-core. Prevents: ledger-core's sole-writer guarantee over ledger_data/ (AD-3) quietly gaining a second writer; a provider transcript being read as domain truth. Rule: sessions, tasks, and the structured agent event stream (session.started, tool.called, task.failed, ...) are appended to agent_data/ as immutable timestamped events, current state a pure projection (AD-3's discipline, different owner and directory). Every event carries timestamp, session_id, provider, mode, event_type, payload (mission_id/task_id once missions exist). Raw provider transcripts, if retained for debugging, live under agent_data/transcripts/ and nothing else consumes them. ledger-core never reads or writes agent_data/. Agent output enters the Ledger only as structured claims through the existing AD-11/AD-12 tools -- 'Claude said X' never becomes 'Rez Ops knows X'.
- (decision) AD-19 (new) Agent provenance on Ledger writes is environment-attested, never caller-supplied. Confirmed by Olu over a caller-supplied argument (rejected: a self-report from the LLM about itself, the same forgeable channel AD-14 closed for approver) and no provenance on records (rejected: a record could not answer plan sec 41's 'which agent/provider wrote this' without a second store). Binds: agent runtime, ledger-core, EvidenceBundle, ActionProposal. Prevents: a fake- or wrong-provider-authored claim being indistinguishable from a real one; the first Ledger field an LLM asserts about itself. Rule: in DRIVEN mode the agent runtime sets fixed env vars (session id, provider, mode=driven) when launching the provider CLI; the CLI spawns ledger-core as a child process so ledger-core reads them from its OWN environment, never from a tool argument -- same never-LLM-suppliable discipline as AD-7 credentials and AD-14's approver. In HOSTED mode no launcher exists, so ledger-core stamps mode=hosted plus the AD-14 operator identity. ledger-core stamps this provenance onto every EvidenceBundle and ActionProposal it creates; a caller-supplied provenance value is a schema violation (AD-9/AD-11 precedent). Fake provider consequence: anything the fake provider authors carries provider=fake by construction, so plan Rule 5 (no fake implementations) is honoured by making the fake loud, not by banning it.
- (decision) AD-20 (new) Provider adapter contract: invariants only, signatures deferred to the Phase 1 spike (plan sec 18: 'design the exact interface after inspecting the actual runtimes; do not prematurely implement fake abstractions'). [ADOPTED from plan sec 19/20/50/87]. Binds: every provider adapter (agents/providers/*), agent runtime core, future routing. Prevents: two adapters diverging on auth handling, failure reporting, or what they emit; a vendor CLI name appearing anywhere outside its adapter; an LLM choosing which provider runs. Rule: (1) an adapter launches the vendor's OFFICIAL local runtime and never extracts, proxies, converts, or stores its credentials -- authentication belongs to the provider runtime; Rez Ops knows only installed/authenticated/available/healthy (AD-17 probe). (2) auth mode is recorded on every execution as SUBSCRIPTION | API, never hidden; LOCAL_SUBSCRIPTION is the primary mode, API is an explicit opt-in. (3) Vendor CLI names, flags, and binaries appear only inside that provider's adapter package -- ledger_core/, connectors/, shared/, and agents/core/ never reference a provider. (4) Every adapter emits the AD-18 event stream and reports failure with an explicit code from one shared enum (AGENT_UNAVAILABLE, AGENT_TIMEOUT, AGENT_QUOTA_EXHAUSTED, AUTHENTICATION_FAILED, ...), never 'unknown error'; a provider that cannot execute leaves the task pending and visible, never silently failed (plan sec 37/49). (5) Provider selection is deterministic and inspectable -- config preferences + AD-17 probe results -- never an LLM's choice; requested vs executed provider are both recorded when they differ. (6) The fake adapter is a real, shipped adapter that satisfies the same contract tests as every other -- it is the contract's executable definition, not a stub.
- (decision) Amend AD-1 in place: 'Voice' is generalised to 'Voice / Agent Runtime' -- whichever MCP-capable agent runtime sits in front of Rez Ops, in HOSTED or DRIVEN mode (AD-16). The plan's rename (sec 18) is adopted conceptually; the spine keeps the Voice name for continuity with 15 existing ADs and stories, and states the equivalence once. Rule unchanged in substance: the runtime orchestrates and presents, holds no domain logic, computes no derived value. New clause: the runtime is also never a system of record -- no provider is load-bearing for Rez Ops's state (plan sec 87).
- (decision) Amend AD-7 in place: (a) the literal 'claude -p --mcp-config .mcp.json --output-format json' clause is demoted to the claude adapter's implementation detail (AD-16/AD-20); the architectural rule is 'scheduled work invokes the agent runtime in DRIVEN mode against .mcp.json'. (b) 'no persistent server process' still holds: the agent runtime is invoked per run by the OS scheduler or the rezops CLI, never a daemon; a mission scheduler that understands types/retry/priority (plan sec 26) is Deferred and must not become a resident process by accident. (c) credential rule extended: provider credentials are owned by the provider runtime and never appear in any Rez Ops config, env var, or log (AD-20); the REZOPS_* env-var namespace is extended with the AD-19 provenance vars, set only by the agent runtime launcher.
- (decision) Structural seed [ADOPTED from plan sec 53/54/63/78]: new top-level agents/ package (core/, providers/{fake,claude,codex,google}/, sessions/) as a peer of ops/ and connectors/; agent_data/ store (AD-18); a rezops console entry point in pyproject [project.scripts] exposing 'rezops doctor' and 'rezops agent run --provider X' -- the first unified CLI, replacing nothing (ops/ scripts stay until a concrete reason moves them). tests/providers/ holds the shared adapter contract suite every adapter including fake must pass (AD-20 clause 6). No wholesale reorganisation; domain/, skills/, ui/, missions/, routing/ are NOT created in Phase 0/1.
- (direction) Deferred (added): Missions and Tasks (plan sec 24/25, Phase 4) -- revisit after Phase 3's exit criterion (each provider answers 'what is DR readiness' via the same MCP tools). CONFLICT to carry forward, surfaced now: the plan's mission shape carries its own 'approvals' field; the spine has exactly one approval mechanism (AD-12 policy_decision + AD-14 approved event). A mission may CITE ActionProposal ids, never own a parallel approval state -- a future mission AD must say so explicitly.
- (direction) Deferred (added): Provider routing implementation (plan sec 22/48, Phase 6) -- AD-20 clause 5 fixes the invariant (deterministic, inspectable, never LLM-chosen, requested-vs-executed recorded); rezops.providers.yaml preferences schema and the router itself are Phase 6. Multi-agent reasoning / roles / independent challenge (sec 23/46/47, Phase 13) -- same. Agent skills (sec 43-45, Phase 5) -- Voice-side prompt packaging, same category as the existing 'specialised reasoning framing' non-AD; revisit only if a skill needs code.
- (direction) Deferred (added): Typed domain model (Application/DRPlan/DRTest/Dependency, plan sec 7, Phase 8), dependency intelligence (sec 13, Phase 9), DR test management (sec 12, Phase 10), Findings and Lessons Learned (sec 15/16, Phase 11), document-content ingestion (sec 33) -- each changes the generic artifact_type/artifact_id substrate or the metadata-only connector boundary and needs its own threat-modelled AD, exactly as roadmap.md already states. Persistence migration (SQLite/DuckDB/Postgres, sec 40) -- revisit only when agent_data/ or ledger_data/ scale forces it; the append-only constraint is kept as a design asset. Hosted/multi-user (sec 77, Phase 15) -- explicit later phase, not a UI side effect. Optional Executor build (Phase 14) -- unchanged from AD-13/14/15.
- (version) Claude Code 2.1.269 (local, 2026-09-12): 'claude -p --mcp-config .mcp.json --strict-mcp-config --output-format stream-json' verified LIVE against this repo -- init event reports all 7 Rez Ops servers connected, 18 mcp__ tools registered. GitHub issue anthropics/claude-code#38987 (2.1.77, 2026-03-25: MCP servers not loaded in --print) does not reproduce on 2.1.269; treated as fixed-or-environmental, which is exactly why AD-17 probes at call time instead of trusting a declaration. Also supports --session-id, --resume, --input-format stream-json, --include-partial-messages.
- (version) OpenAI Codex CLI (web, 2026-09-12; learn.chatgpt.com/docs/non-interactive-mode): headless = 'codex exec "task"' (progress to stderr, final message to stdout), '--json' JSONL event stream (thread.started/turn.*/item.*/error), '--output-schema <path>' for schema-conformant final output, '-o' last-message file, 'codex exec resume --last | <SESSION_ID>', '--ephemeral'. ChatGPT-subscription auth via 'codex login' (device auth available), tokens in ~/.codex/auth.json -- Rez Ops never reads that file (AD-20). Sandbox default read-only; MCP servers declared in config.toml (/etc, ~/.codex, project .codex/), 'required = true' makes exec fail loudly if a server fails to init. LOCAL: npm @openai/codex 0.72.0 wrapper present but vendor binary missing (spawn ENOENT) -- a live example of installed != healthy, the case AD-17's probe exists for.
- (version) Google (web, 2026-09-12): Gemini CLI STOPPED serving Google AI Pro/Ultra/free requests on 2026-06-18 (developers.googleblog.com transition post; Antigravity CLI available since 2026-05-19). The subscription-backed terminal agent is now Antigravity CLI, binary 'agy' (curl install from antigravity.google/cli/install.sh). Headless: '-p/--print', '--output-format text|json|stream-json', '--continue'/'--conversation <ID>', '--input-format stream-json' multi-turn stdin; auth = cached credentials from one prior interactive login, unauthenticated headless exits non-zero 'authentication required' (never hangs). MCP config: ~/.gemini/config/mcp_config.json or workspace .agents/mcp_config.json, SAME mcpServers/command/args JSON shape as .mcp.json, no CLI flag for an alternate path. Headless permissions: tools soft-deny unless permissions.allow in ~/.gemini/antigravity-cli/settings.json (or --dangerously-skip-permissions, which Rez Ops must never use). Gemini CLI stays open-source and works for enterprise Code Assist licences / API keys only. Plan sec 63's warning ('do not assume the old Gemini CLI remains the individual-subscription route') is confirmed correct. LOCAL: gemini 0.36.0 installed (obsolete for subscription use), agy not installed.
- (version) MCP Python SDK (web + local, 2026-09-12): 2.2.0 released 2026-09-07 and is now the default 'pip install mcp' (2.0.0 GA 2026-07-28, spec 2026-07-28; new MCPServer API, multi-round-trip requests/resolver DI, OTel default). 1.x line: 1.30.0 (2026-09-07), security fixes only. Project pins mcp>=1.29,<2 with 1.29.0 locked. Decision: keep the <2 pin for Phase 0/1 -- provider adapters launch CLIs and never import the SDK, so nothing in this amendment touches it; but the Deferred 'MCP SDK v2 migration' item is upgraded from 'too new' to 'revisit before Phase 3 (Agent<->MCP integration)', since v2 is now the stable default and the elicitation API AD-13 leans on is where 1.x/2.x differ most.
- (decision) Amend AD-16 (MCP config derivation): the three verified runtimes do NOT share an MCP config surface -- Claude Code reads .mcp.json via --mcp-config; Codex reads TOML (config.toml, project .codex/); Antigravity reads .agents/mcp_config.json (same JSON shape, no path flag). Rule tightened: .mcp.json is the SINGLE source of truth for the Rez Ops server set in both modes; in DRIVEN mode each adapter DERIVES its provider's native MCP configuration from .mcp.json at launch (generated into a per-run location, never hand-maintained, never committed as a second copy). Two hand-maintained server lists is the divergence this prevents. Exact derivation mechanics per provider are Phase 1 spike detail.
- (decision) Amend AD-20 (add clause 7, tool scope in DRIVEN mode): a driven provider session is granted exactly the Rez Ops MCP tools (mcp__<server>__* for servers in .mcp.json) and nothing else -- the provider's built-in shell, file-write, and web tools are denied via the provider's own permission mechanism (Claude: allowed-tools/permission mode; Codex: read-only sandbox + MCP allow; Antigravity: permissions.allow rules). No adapter ever passes a skip-all-permissions flag. Rationale: plan sec 38 rules 3/4/7 (external content untrusted, agent output untrusted, no hidden execution path) -- a driven agent reading a prompt-injected SharePoint title must have no path to act outside the Ledger's gated tools. HOSTED mode is the human's own session and is not constrained by this clause.
- (event) Spine distilled from memlog: AD-16..AD-20 added, AD-1/AD-7 amended in place, Design Paradigm + both diagrams + Consistency Conventions (new agent-runtime row) + Stack (Claude Code 2.1.269 live-verified, Codex CLI, Antigravity CLI agy, mcp SDK 1.30/2.2) + Structural Seed (agents/, agent_data/, docs/, tests/providers/, rezops CLI) + Deferred (10 new items, MCP v2 item upgraded, multi-user item folded into hosted-deployment item) updated. lint_spine: 7 low placeholder hits, all path-pattern tokens ({evidence_id}, {artifact-type}, {session_id}) -- same accepted false-positive class as prior runs. Reviewer gate dispatched: reconcile-plan, rubric-walker, version-currency, adversarial.
- (event) Reviewer gate, version-currency (AD-16..20): CONDITIONAL PASS, every hard claim confirmed live. Applied: F1 Antigravity CLI is GA v1.2.0 (not preview) -- Stack row reworded with date; F2 .agents/mcp_config.json shape matches .mcp.json for stdio entries only (all 7 Rez Ops servers are stdio) -- AD-16 + Stack row note; F4 Antigravity headless exits 0 / status SUCCESS even on a denied tool call -- AD-17 now says judge health from the structured result never exit code alone, Stack row carries the specific warning; F3 elicitation Deferred item reworded (1.x ctx.elicit vs 2.x resolver form; uv.lock at 1.29.0 vs 1.30.0 current -- flagged as Phase 0 housekeeping, NOT bumped in this run since Phase 0 exit criterion is no production change). INFO F7: the 'installed but binary missing' Codex example is literally true on this machine (@openai/codex 0.72.0 wrapper, npm latest 0.154.0).
- (decision) Reviewer gate, reconcile-plan (AD-16..20): CONDITIONAL PASS, 2 HIGH fixed. HIGH-1: AD-20 s7 granted driven sessions every ledger-core tool incl. ledger_ingest_raw_fact(source: str freeform) and ledger_create_draft, neither covered by AD-19 -- a driven LLM could append a RawFact with an invented source (plan sec 28's 'Claude said X -> Rez Ops knows X'), and AD-18's 'enters only via AD-11/12 tools' sentence was false (verified: 4 write tools exist in ledger_core/server.py). Fix: AD-19 now stamps provenance on EVERY record a write tool creates (RawFact ingest, Draft, EvidenceBundle, ActionProposal); AD-18 sentence corrected to name all four tools and state a relayed fact is recorded as relayed-by-session; new Deferred item names the LLM-free Sensor->Ledger relay as a future AD-1 amendment rather than pretending it exists. HIGH-2: AD-19 needs ledger_core/ changes but Phase 1 says do not modify Ledger -- paradigm + AD-19 now assign the stamp (and AD-12 policy-file SHA-256 on decided, and the shared failure enum) to Phase 3; Phase 1 only sets env vars. MEDIUM: failure enum is now ONE system-wide enum in shared/failures (connector/ledger/agent/policy codes) not adapter-only; a driven launch whose .mcp.json servers do not all attach fails with MCP_ATTACH_FAILED, never runs silently degraded. MEDIUM: no secrets in agent_data -- payloads carry tool names + argument digests, transcripts opt-in and git-ignored (default off), sessions/ committed. MEDIUM: 'what policy version' -- decided event records SHA-256 of policy + tiers yaml (Phase 3). O-1: env inheritance reworded as a per-adapter obligation verified in the spike; adapter that cannot guarantee it fails launch with PROVENANCE_UNAVAILABLE. Lower: SUBSCRIPTION named as the plan's LOCAL_SUBSCRIPTION; 'never bypass vendor rate/usage limits' added to s1; model field added to events + provenance; fake adapter doubles as fault injector (plan sec 52).
- (decision) Reviewer gate, rubric-walker (AD-16..20): CONDITIONAL PASS, 4 HIGH (2 overlapping reconcile-plan, already fixed: all 4 write tools stamped; Phase 3 owns ledger-core half). New fixes: HIGH AD-7<->AD-18 contradiction -- a failed driven run is now a session.failed event in agent_data + non-zero exit, _ops.log.md stays the failure log only for legacy ops/ scripts; ops/run_scheduled_briefing.py (hard-codes claude -p) is a NAMED grandfathered exception to AD-20 s3 until Phase 1 makes it a shim over 'rezops agent run --provider claude'. HIGH AD-19 hosted operator -- AD-14 was design-only with no variable; REZOPS_OPERATOR is now named in both AD-14 and AD-19 (absent -> operator: unknown). HIGH AD-19 brownfield read-compat -- evidence._parse_bundle_file / action_proposals.list_action_proposals strictly reject missing keys; provenance is now required-on-write, optional-on-read (legacy -> provenance: null, never rejected, never confidence-0 sentinel). MEDIUM AD-20 s6 -- fake defined three incompatible ways; now: in-process, no binary, the ONE exemption from s1, reaches MCP tools via a real client with AD-19 env (Phase 3); Phase 1 story order fixed: core+fake+suite, then claude, then codex/google. MEDIUM -- session_id is Rez Ops-assigned UUID before launch, provider-native id recorded as provider_session_id; derived MCP configs are git-ignored and a provider's GLOBAL MCP config may never merge servers into a driven session; agent_data durability follows AD-7 exactly, and the reviewer's observation that ledger_data/ has zero tracked files (AD-7's push step was never automated) is recorded in the git-strategy Deferred item rather than hidden.
- (decision) Reviewer gate, adversarial (AD-16..20): 2 CRITICAL, 4 HIGH. CRIT-1 (AD-7 vs AD-18 + second headless launcher in ops/) -- already fixed via rubric-walker pass (session.failed in agent_data; ops script a named grandfathered exception until it becomes a shim). CRIT-2 tool scope escapable via provider GLOBAL MCP config merge (this machine's ~/.codex/config.toml registers a node_repl server a driven Codex session would inherit; Claude needs --strict-mcp-config which no AD required; scope was defined in Claude's mcp__ naming, violating s3). Fix: AD-20 s7 now provider-neutral (server,tool) set from .mcp.json, enforced by mandatory strict/isolated MCP-config mode + built-in tool denial + loaded-server-set verification before first task (TOOL_SCOPE_VIOLATION). HIGH-3 env inheritance provider-specific (Codex whitelists env for MCP children; obvious workaround writes credential VALUES into a generated file). Fix: AD-19 forward env by NAME never by value; cannot -> PROVENANCE_UNAVAILABLE; partial/malformed REZOPS_AGENT_* -> PROVENANCE_INVALID, write refused, never falls open to hosted; end-to-end stamp test in contract suite. HIGH-4 (unstamped ingest/draft) already fixed. HIGH-5 cwd/home: adapters launch provider with project root as cwd (ledger-core resolves ledger_data/ relative to cwd today), never relocate provider home (CODEX_HOME would move auth), derived config at git-ignored .rezops/run/{session_id}/ so concurrent sessions never clobber; concurrent ledger_data writes stay the pre-existing no-locking Deferred item. HIGH AD-18: event line grammar = ledger_data/*.log.md's (ledger_core/log.py reference), payload shapes defined once/versioned in agents/core, added task.pending (home of 'pending'), session.cancelled, session.failed. All reviewer findings now applied; lint unchanged (7 low path-token false positives).
- (event) Reviewer-gate tail (medium/low across all four reviews) applied: AUTH_MODE_MISMATCH -- auth_mode is probed and an ambient vendor API key never silently flips a SUBSCRIPTION session to API without per-provider opt-in (adversarial F8); provider process lives only for one 'rezops agent run' under AGENT_TIMEOUT, resume re-launches via native resume + Rez Ops session_id, so no de-facto daemon (adversarial F9); rezops doctor probes without a session by default, --live is a full driven session under every AD (adversarial F12); failure enum is a closed set in code, ellipses are elision (adversarial F11); fake's MCP reach is always against an isolated test root (adversarial F7); 'never scrapes a private web session' added to s1 (reconcile DNL-7); untrusted-content convention made explicit (reconcile DNL-6); agent.message event added, approval.requested deferred to Missions (reconcile DNL-12); Deferred item for observability/timeout default/security-model.md (rubric F11, reconcile DNL-11). Not applied (low, cosmetic): enum casing mix in provenance block (rubric F9) -- kept, mode lowercase and auth_mode uppercase mirror the plan's own spelling; CLI install mechanics (rubric F10) -- seed detail for the Phase 1 story. GATE CLOSED: lint 9 low path-token false positives, all critical/high/medium findings resolved in place.
- (event) spine finalized (AD-16..20 amendment). ARCHITECTURE-SPINE.md: 20 ADs (15 prior + AD-16 two modes/one MCP surface, AD-17 probed capabilities, AD-18 agent_data store, AD-19 environment-attested provenance on every write, AD-20 adapter contract with 7 clauses), AD-1/AD-7/AD-12/AD-14 amended in place, status final, updated 2026-09-12. Reviewer gate: lint 9 low path-token false positives; reconcile-plan CONDITIONAL PASS (2 high fixed), rubric-walker CONDITIONAL PASS (4 high fixed), version-currency CONDITIONAL PASS (1 medium fixed), adversarial 2 critical + 4 high fixed; all medium tails applied. Phase 0 exit criteria verified: 820 tests pass, zero production files changed (diff confined to _bmad-output/ and docs/). Renderings: docs/architecture-next.md + docs/provider-adapter-contract.md (new, Phase 0 deliverables), solution-design.md + walkthrough-deck.html (new Agent Runtime slide, Stack/Deferred/Closing updated) -- doc-standards polish (bmad-review structure+prose) dispatched on the prose only, spine untouched by it. Branch feat/rez-ops-next-generation; nothing committed by this run.
- (event) Doc-standards polish (bmad-review structure+prose) applied to prose only, spine untouched: solution-design.md 8662->8566 words (new AD-16..20 section 1076->954, closing 'withheld' paragraph condensed since it duplicated same-day Deferred entries; new brief-mapping paragraph moved after the AD-11..15 exception so exceptions read chronologically; 'fortnight before this session' corrected to dated facts -- Gemini cutoff was 2026-06-18, three months ago, the author's wording conflated discovery with occurrence); docs/architecture-next.md 1541->1559 (same date fix; 'the plan's own phrase' corrected to 'the earlier amendment's phrase' -- product-direction.md does not contain the door/wall line; cross-refs added to contract s11/s12); docs/provider-adapter-contract.md 1687->1701 (plainer wording in s8/s9, no rule changed). All AD refs, failure codes, dated vendor facts, checklist items verified unchanged. Run complete on branch feat/rez-ops-next-generation. Nothing committed. Next: bmad-spec to adopt AD-16..20 as CAPs + Phase 1 stories.
- (decision) Amend AD-20 s4 in place (post-spec reconciliation, 2026-09-12): the one system-wide failure enum is housed in agents/core during Phase 1 (which may not touch shared/) and relocated, same closed set, into shared/ in Phase 3 beside connector/ledger/policy codes. Seed's agents/core comment updated to match. Trigger: bmad-spec surfaced that s4's literal 'defined once in shared/' was unsatisfiable by any Phase 1 adapter while the seed already listed the enum under both locations; Olu resolved it agents/core-first in the spec, so the spine now says the same thing.
