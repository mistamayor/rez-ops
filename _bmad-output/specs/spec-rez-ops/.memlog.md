---
topic: Rez Ops v1 -- AI agent framework for Disaster Recovery program management
updated: 2026-09-12T16:33
---

- (note) Input: structured, pre-sorted upstream artifacts. Sources (fully absorbed): brief.md + addendum.md (brief-Resillience-Ops-2026-08-12). Adopted companions (downstream still needs): ARCHITECTURE-SPINE.md + solution-design.md (architecture-Resillience-Ops-2026-08-12) -- owned by bmad-architecture, not edited here. walkthrough-deck.html excluded: presentation aid, not load-bearing for implementation
- (capability) CAP-1 Freshness Ledger -- intent: maintain a structured, per-artifact-type record of verification state (last verified, method, expiry rule, tier SLA, escalation owner) that expires rather than staying valid indefinitely. success: every tracked artifact type has a queryable ledger record whose fields are populated or explicitly marked unknown, with an expiry rule that can trigger a staleness flag
- (capability) CAP-2 Read-only connectors -- intent: ingest calendar, ticketing, git, and CMDB data as raw observed facts without writing back to any of those systems. success: each connector returns normalized raw facts to the runtime; no connector call in v1 issues a write/update/delete against its external system
- (capability) CAP-3 Confidence/coverage computation -- intent: compute an explicit confidence state (agent-verified, manual, or unknown) for every derived ledger value rather than presenting derived state as uniformly certain. success: a coverage query returns confidence broken down by domain and tier; any record lacking a verifying source shows as unknown, never omitted or defaulted
- (capability) CAP-4 Chat-queryable live state -- intent: let a user ask, in natural language, what is stale, what is due, or what needs their attention, and get an answer from current ledger state on demand. success: a live query returns an answer reflecting current state without requiring a scheduled briefing to have run first
- (capability) CAP-5 Ownership inference and arbitration -- intent: infer who is accountable for an entity from live activity signals rather than a static ownership record, and arbitrate disagreement between sources. success: for any ownership-bearing field with conflicting inputs, exactly one source is authoritative and the conflict is recorded, never silently overwritten; total absence of signal marks the entity orphan-risk
- (capability) CAP-6 Draft-not-send outbound content -- intent: draft outbound content (e.g. an owner-reconfirmation message) for human review without sending it. success: every drafted item is retrievable from a pending queue and requires explicit human action before anything sends externally; v1 has no auto-send code path
- (capability) CAP-7 Daily/periodic briefing -- intent: produce a periodic briefing ranking what needs a decision today, using the same underlying state a live query would return. success: a generated briefing's content matches what a live query returns at the same point in time; delivery channel swaps without changing the underlying content
- (capability) CAP-8 Scheduled headless operation -- intent: generate a briefing on a schedule without a human present. success: an OS-scheduled invocation completes end-to-end and updates ledger state or logs a failure explicitly; no scheduled run fails silently
- (constraint) No connector may write to any external system of record in v1 (read-only-first)
- (constraint) Any Rez Ops failure (connector or ledger-core outage) must fail open to today's manual baseline -- never blocking or worsening the DR program (graceful degradation)
- (constraint) Every derived/computed value must carry an explicit confidence state; no derived value may be presented without one (never hide uncertainty)
- (constraint) v1 runs local-first: no hosted database, container orchestration, or persistent server process; git is the sole persistence layer
- (constraint) Ledger state is mutated only through an append-only log; no in-place edits -- single writer, auditable history
- (constraint) v1 favors fewer, high-trust, provenance-ranked sources over broad source coverage
- (direction) Non-goal: write-back or auto-actioning against any system of record
- (direction) Non-goal: a retroactive would-have-caught-it scoreboard against past incidents (Blast Radius Rewind)
- (direction) Non-goal: proactive or surprise micro-drills / agent-triggered synthetic failovers
- (direction) Non-goal: auto-sending owner-reconfirmation messages or any outbound content without human approval
- (direction) Non-goal: a passive-observation, watch-only baselining period
- (direction) Non-goal: multi-user or multi-tenant support
- (direction) Non-goal: packaging as an installable product for other practitioners -- left open, not decided for v1
- (direction) Non-goal: migrating to MCP SDK v2 in v1
- (decision) Success signal: zero 'we didn't know that was stale' surprises at the next audit or review; who-owns-X answered in seconds via a live query instead of days of chasing; a generated briefing consistently surfaces genuinely new information rather than repeating already-handled items
- (assumption) Primary user and scale modeled on a Booking.com/Expedia-scale global travel/e-commerce business; real target scale otherwise unconfirmed
- (assumption) The refuse-to-be-a-system-of-record differentiator is a product-philosophy choice, not a defensible technical moat, and could be replicated by a competitor
- (question) Is Rez Ops a personal configuration for a single program owner, or does it need to support installation by other DR practitioners? Affects how generic connector configuration and onboarding need to be
- (question) Which specific calendar/ticketing/CMDB vendor products will the first connectors target?
- (question) Preservation-pass addition: brief's secondary-users assumption (compliance stakeholders, application/service owners) has no home yet -- should v1 capabilities design for them directly, or stay scoped to the primary program-owner user until the CAP set stabilizes?
- (event) Self-Validate Pass 1 (coherence, rules 1-6+8): PASS. All 8 capabilities carry intent+success; intents are WHAT not HOW (mechanism deferred to ARCHITECTURE-SPINE.md); all 6 constraints bend a real design decision; 8 non-goals present; success signal is concrete/demonstrable (matches brief's own stated criteria verbatim in spirit); CAP-1..8 are fresh stable IDs; kernel prose is lean, no decoration found
- (event) Self-Validate Pass 2 (preservation): walked brief.md + addendum.md claim by claim. All Scope/Non-negotiables/Success-Criteria/Who-This-Serves-scale/packaging claims landed in a capability, constraint, non-goal, success signal, or assumption/open-question. One gap found and fixed: secondary-users consideration had no home -- added as an open question. Wrapper-only content (correctly dropped, no fix needed): competitive-landscape narrative and the optional internal-vocabulary naming (OwnerGraph/Antibody Ledger/Risk Forecast/Black Box Briefing) -- addendum.md itself frames the vocabulary as optional/discardable, and neither the architecture spine nor this spec adopted those names, which is a valid discard, not a drop
- (decision) Story 1 -- Shared schema + ledger-core foundation. spec_checkpoint=true, done_checkpoint=true (foundation, review before and after). Covers CAP-1 plumbing, AD-3/AD-4/AD-9. No real connector yet, synthetic facts only
- (decision) Story 2 -- First connector: git. spec_checkpoint=false, done_checkpoint=false. Covers CAP-2 partial, AD-1/AD-2 (Sensor pattern, domain-prefixed tool naming). Establishes the pattern Story 5 repeats
- (decision) Story 3 -- Confidence and coverage computation. spec_checkpoint=false, done_checkpoint=false. Covers CAP-3, AD-5, using Story 2's connector as real data source
- (decision) Story 4 -- Chat-queryable live state. spec_checkpoint=false, done_checkpoint=false. Covers CAP-4, runtime/Voice-layer query surface per AD-1
- (decision) Story 5 -- Remaining connectors: calendar, ticketing, CMDB. spec_checkpoint=false, done_checkpoint=false. Covers CAP-2 completion, repeats Story 2's AD-2 pattern for 3 more domains
- (decision) Story 6 -- Ownership inference and arbitration. spec_checkpoint=true, done_checkpoint=true (wedge feature, review before and after). Covers CAP-5, AD-10 per-field ownership map + orphan-risk escalation. Needs 2+ connectors reporting an overlapping field to exercise for real
- (decision) Story 7 -- Draft-not-send outbound content. spec_checkpoint=false, done_checkpoint=false. Covers CAP-6, AD-6 write-tool-only drafts boundary
- (decision) Story 8 -- Periodic briefing. spec_checkpoint=false, done_checkpoint=false. Covers CAP-7, must match Story 4's live-query content at the same point in time
- (decision) Story 9 -- Scheduled headless operation. spec_checkpoint=false, done_checkpoint=false. Covers CAP-8, AD-7 headless invocation (claude -p --mcp-config .mcp.json --output-format json, never --bare), explicit failure logging
- (event) User confirmed the 9-story breakdown and split/order as proposed; confirmed checkpoint placement at Story 1 and Story 6 (both spec_checkpoint and done_checkpoint), no checkpoints elsewhere
- (event) stories.yaml rendered and checked against stories-schema.md validity rules: 9 entries, unique prefix-free string ids '1'-'9', no status field -- all pass
- (note) Reconciliation triggered by bmad-build execution drift: original id 5 ('remaining connectors: calendar, ticketing, CMDB') was split during build into three separately-shipped, separately-pinned stories (5-ticketing-connector-servicenow.md, 6-calendar-connector-google.md, 7-cmdb-connector-servicenow.md, all status: done). stories.yaml's ids 5/6/7 title/description still described the pre-split scope, colliding in meaning with what's actually pinned there per stories.yaml's own id-pinning rule
- (decision) Amend pinned id '5' description in place (cannot renumber, spec file exists): now 'Ticketing connector: ServiceNow' -- ticketing_get_ticket_status, split off from the original bundled scope since each connector is independently shippable
- (decision) Amend pinned id '6' description in place: now 'Calendar connector: Google Calendar' -- calendar_get_event_status, further split from a 'both Google and Microsoft 365' ask; Microsoft 365 tracked in deferred-work.md
- (decision) Amend pinned id '7' description in place: now 'CMDB connector: ServiceNow' -- cmdb_get_ci_status, the fourth and last connector from the original brief's brainstormed scope, closing out SPEC CAP-2
- (decision) Renumber the four ideas displaced by the id-5 split (originally ids 6-9: ownership arbitration, draft-not-send, periodic briefing, scheduled headless) to ids 8-11, since 6/7 are now permanently pinned to calendar/CMDB and 8/9's old content moves up too. Original order preserved (ownership arbitration first, the brief's wedge feature, per the original dependency reasoning) -- none of ids 8-11 had a spec file yet, so free to renumber per stories.yaml's own update semantics
- (decision) Story 8 (was 6) -- Ownership inference and arbitration. spec_checkpoint=true, done_checkpoint=true (wedge feature, unchanged from original). Covers CAP-5, AD-10 per-field ownership map + orphan-risk escalation. Needs 2+ connectors reporting an overlapping field to exercise for real -- now satisfied, four connectors exist (git, ticketing, calendar, CMDB)
- (decision) Story 9 (was 7) -- Draft-not-send outbound content. spec_checkpoint=false, done_checkpoint=false (unchanged). Covers CAP-6, AD-6 write-tool-only drafts boundary
- (decision) Story 10 (was 8) -- Periodic briefing. spec_checkpoint=false, done_checkpoint=false (unchanged). Covers CAP-7, must match Story 4's live-query content at the same point in time
- (decision) Story 11 (was 9) -- Scheduled headless operation. spec_checkpoint=false, done_checkpoint=false (unchanged). Covers CAP-8, AD-7 headless invocation (claude -p --mcp-config .mcp.json --output-format json, never --bare), explicit failure logging
- (event) stories.yaml re-derived and re-checked against stories-schema.md validity rules: 11 entries, unique prefix-free string ids '1'-'11', no status field -- all pass. Cross-checked against pinned files on disk: ids 1-7 now match their real pinned spec files exactly (title/description corrected for 5/6/7); ids 8-11 (ownership arbitration, draft-not-send, periodic briefing, scheduled headless) are unpinned and collision-free, ready for their spec files to be created next
- (event) Bootstrap: no .memlog.md existed for this spec folder (SPEC.md/stories.yaml predate that convention, authored directly by an earlier bmad-spec/bmad-create-epics-and-stories pass). Baseline entries below capture the current SPEC.md verbatim so it re-derives faithfully going forward; this run's actual new work is the CAP-9/CAP-10 entries appended after the baseline.
- (decision) Why: A pain to solve, for DR/resilience program owners at large, always-on organizations: DR program artifacts (BIAs, tiering, runbooks, RACI, test schedules) are scattered across disconnected systems with no shared concept of freshness, so reconstructing the program's true state is manual and slow -- done only under pressure, right before an audit or right after an incident proves a gap was real. Rez Ops closes that gap as a thin, read-only orchestration layer over the tools a program already has, rather than a new system of record, so the program becomes proactive instead of reactive.
- (capability) CAP-1 -- Freshness Ledger. intent: Maintain a structured, per-artifact-type record of verification state (last verified, method, expiry rule, tier SLA, escalation owner) that expires rather than staying valid indefinitely. success: Every tracked artifact type has a queryable ledger record whose fields are populated or explicitly marked unknown, with an expiry rule that can trigger a staleness flag. [DONE -- Story 1]
- (capability) CAP-2 -- Read-only connectors. intent: Ingest calendar, ticketing, git, and CMDB data as raw observed facts without writing back to any of those systems. success: Each connector returns normalized raw facts to the runtime; no connector call in v1 issues a write, update, or delete against its external system. [DONE -- Stories 2, 5, 6, 7]
- (capability) CAP-3 -- Confidence/coverage computation. intent: Compute an explicit confidence state (agent-verified, manual, or unknown) for every derived ledger value rather than presenting derived state as uniformly certain. success: A coverage query returns confidence broken down by domain and tier; any record lacking a verifying source shows as unknown, never omitted or defaulted. [DONE -- Story 3]
- (capability) CAP-4 -- Chat-queryable live state. intent: Let a user ask, in natural language, what is stale, what is due, or what needs their attention, and get an answer from current ledger state on demand. success: A live query returns an answer reflecting current state without requiring a scheduled briefing to have run first. [DONE -- Story 4]
- (capability) CAP-5 -- Ownership inference and arbitration. intent: Infer who is accountable for an entity from live activity signals rather than a static ownership record, and arbitrate disagreement between sources. success: For any ownership-bearing field with conflicting inputs, exactly one source is authoritative and the conflict is recorded, never silently overwritten; total absence of signal marks the entity orphan-risk. [DONE -- Story 8]
- (capability) CAP-6 -- Draft-not-send outbound content. intent: Draft outbound content (e.g. an owner-reconfirmation message) for human review without sending it. success: Every drafted item is retrievable from a pending queue and requires explicit human action before anything sends externally; v1 has no auto-send code path. [DONE -- Story 9]
- (capability) CAP-7 -- Daily/periodic briefing. intent: Produce a periodic briefing ranking what needs a decision today, using the same underlying state a live query would return. success: A generated briefing's content matches what a live query returns at the same point in time; the delivery channel swaps without changing the underlying content. [DONE -- Story 10]
- (capability) CAP-8 -- Scheduled headless operation. intent: Generate a briefing on a schedule without a human present. success: An OS-scheduled invocation completes end-to-end and updates ledger state or logs a failure explicitly; no scheduled run fails silently. [DONE -- Story 11]
- (event) Correction: the CAP-1..8 baseline entries just appended above were unnecessary duplicates -- .memlog.md already existed with the full original derivation history (52 entries, 2026-08-12 through 2026-08-23), just hidden from an ls listing that didn't show dotfiles. No harm done (restated facts, not contradictions), but noting it so a future reader isn't confused by the doubled capability lines. Proceeding with this run's actual new work below: distilling AD-11 (Evidence boundary) and AD-12 (ActionProposal + Policy Engine) from the amended ARCHITECTURE-SPINE.md (updated 2026-09-05) into new capabilities.
- (capability) CAP-9 -- Evidence-backed claims. intent: Let Voice make a reasoning-layer claim about ledger state as a structured EvidenceBundle (claim, confidence, cited facts, reasoning) rather than unattributed prose, with confidence computed exclusively by ledger-core from the cited evidence -- never supplied by the caller. success: Every EvidenceBundle's confidence is ledger-core-computed, not accepted as input; a caller-supplied confidence value is rejected as a schema violation; every citation resolves back to a real ingested fact or ledger record, never an inline duplicate. Implements AD-11.
- (capability) CAP-10 -- Policy-gated action proposals. intent: Let Voice propose a system-state-changing action -- naming it from a config-declared vocabulary, citing at least one EvidenceBundle -- and have ledger-core alone compute whether it's automatic, requires human approval, or is denied, without any component executing it. success: Every ActionProposal's policy_decision is computed by ledger-core from config-declared action risk, target criticality, and the minimum confidence across cited evidence -- never asserted by the caller; naming an undeclared action or citing no evidence is rejected before anything is recorded; no code path in v1 consumes an approved/automatic decision to perform the action against any external system. Implements AD-12.
- (direction) Amend Non-goal 'Write-back or auto-actioning against any system of record' in place: still true (nothing in v1 executes an action), now coexisting with CAP-10's propose-and-evaluate-only capability -- a computed policy_decision is a recorded judgment, not a permission that anything acts on.
- (direction) New Non-goal: an Executor that consumes an ActionProposal's policy_decision to actually perform the action against any external system -- explicitly deferred per AD-12's own Never clause; a separate, later, deliberate decision, not a default extrapolation from CAP-10 existing.
- (decision) Success signal addition: every proposed action shows its evidence trail and policy decision before any human is asked to approve it -- never a bare recommendation with no attributable reasoning.
- (constraint) Voice may propose a claim or an action; it never computes the derived value that evaluates it -- EvidenceBundle.confidence and ActionProposal.policy_decision are ledger-core-exclusive, extending CAP-3's confidence-computation discipline to the proposal layer.
- (event) Self-Validate Pass 1 (coherence, rules 1-6+8) on the CAP-9/CAP-10 addition: PASS. Both new capabilities carry intent+success; intents describe WHAT (object shape, who computes what) not HOW (mechanism deferred to AD-11/AD-12), consistent with CAP-1's existing level of detail; the new constraint (ledger-core-exclusive computation of confidence/policy_decision) bends a real design decision the same way CAP-3's constraint already does; 2 new non-goals added, non-goals list still non-empty and each rules something out; success signal addition is demonstrable (evidence trail + policy decision visible before approval); CAP-9/CAP-10 are fresh stable IDs continuing from CAP-8; prose density matches the existing kernel's style.
- (event) Self-Validate Pass 2 (preservation) on this update: walked ARCHITECTURE-SPINE.md's AD-11 and AD-12 claim by claim against the new SPEC.md content. Every load-bearing claim landed: EvidenceBundle's shape and ledger-core-only confidence computation (CAP-9 + new constraint); ActionProposal's fixed vocabulary source, ledger-core-only policy_decision computation, minimum-confidence-aggregation rule, and no-executor guarantee (CAP-10 + amended/new non-goals). Wrapper-only content correctly left in ARCHITECTURE-SPINE.md/solution-design.md, not duplicated into the kernel: the exact EvidenceRef/ActionProposal field-level shapes, the storage model (one-file-per-bundle vs. append-only log), and the full narrative of which reviewer found which gap -- all HOW/mechanism detail the companions already own.
- (decision) Story 12 -- Evidence boundary. spec_checkpoint=true, done_checkpoint=true (user-confirmed: new, review-sensitive ground -- the self-approval loophole was only just caught at the architecture level). Covers CAP-9, implements AD-11: EvidenceBundle schema, ledger-core-only confidence computation, one-file-per-bundle storage.
- (decision) Story 13 -- ActionProposal and the Policy Engine. spec_checkpoint=true, done_checkpoint=true (user-confirmed: the story guaranteeing the LLM never decides for itself whether it's allowed to act). Covers CAP-10, implements AD-12: fixed action vocabulary sourced from rezops.policy.yaml, ledger-core-only policy_decision computation (action risk x target criticality x minimum cited-evidence confidence), append-only proposed/decided event log, no executor. Depends on Story 12's EvidenceBundle existing (every ActionProposal must cite at least one).
- (event) User confirmed the 2-story breakdown for CAP-9/CAP-10 (Story 12: Evidence boundary, Story 13: ActionProposal/Policy Engine) and confirmed both spec_checkpoint and done_checkpoint=true for each.
- (event) stories.yaml re-derived and checked against stories-schema.md validity rules: 13 entries, unique prefix-free string ids '1'-'13', no status field -- all pass. Ids 12/13 (Evidence boundary, ActionProposal & Policy Engine) have no existing stories/ spec file, confirmed collision-free and free to renumber if a future breakdown needs to.
- (event) bmad-spec run complete on branch feature/policy-and-evidence: SPEC.md now has CAP-9/CAP-10, an amended non-goal pair (auto-actioning / no-Executor), a new constraint (ledger-core-exclusive computation of confidence/policy_decision), and a success-signal addition. stories.yaml has 2 new entries (12, 13), both spec_checkpoint=true/done_checkpoint=true per user confirmation. Self-Validate passes 1 and 2 both PASS.
- (event) Story 12 (Evidence boundary) done: 3-layer review found a real, converged bug (source-citation resolution ignored artifact_id, letting one artifact's citation resolve against a different artifact's real fact -- confirmed by two reviewers, one with a concrete repro proving no existing test caught it) plus a field-citation whitelist gap and a citation-dedup gap, all fixed. ARCHITECTURE-SPINE.md's AD-11 text also corrected in the same pass (documentation-fidelity gap the review surfaced: the spine still described the superseded shared/ledger_schema/ placement and the pre-artifact_id EvidenceRef shape). 447 tests passing.
- (event) Story 13 (ActionProposal and the Policy Engine) done: 3-layer review converged well -- two reviewers independently caught the same evidence-type foot-gun (a bare string silently iterating per-character), plus a real availability gap (a corrupted action_proposals.log.md would permanently block all future proposal creation, not just reads), policy-parser strictness inconsistencies, and two corrupted-log-read hardening gaps -- all fixed and regression-tested (the implementer's own fault-injection check caught one initially-weak test and strengthened it). Also caught: README.md's tool count was stale since Story 12 (still said 7 tools); now lists all 11. 494 tests passing. This closes stories.yaml -- all 13 stories now done.
- (direction) CAP-2 intent widened: 'Ingest calendar, ticketing, git, and CMDB data' -> 'Ingest calendar, ticketing, git, CMDB, and document-store (Google Drive, SharePoint) data' -- adding document-freshness connectors to close a real gap Olu identified: BIAs/RACI matrices/tiering docs often live as SharePoint/Google Drive files, not git/ServiceNow/Calendar/CMDB records, so today's four connectors can't observe them directly. Success criterion unchanged -- it was already generic across any connector.
- (decision) Story 14 -- Google Drive connector. spec_checkpoint=false, done_checkpoint=false (routine connector story, same weight as Stories 5/6/7). Covers CAP-2 extension: google_drive_get_document_status(file_id, artifact_type, artifact_id), last-modified metadata only (modifiedTime, lastModifyingUser flattened to a scalar), REZOPS_DRIVE_TOKEN credential separate from REZOPS_CALENDAR_TOKEN despite same Google OAuth ecosystem (AD-7 no-credential-sharing-even-same-vendor). Confirmed by Olu: build both Google Drive and SharePoint in sequence (not split-and-defer like the original calendar split), Google Drive first since it mirrors the existing Google Calendar connector's proven OAuth pattern.
- (decision) Story 15 -- SharePoint connector. spec_checkpoint=false, done_checkpoint=false. Covers CAP-2 extension: a Microsoft Graph API-based document-status connector, last-modified metadata only, matching Story 14's shape but a new auth pattern for this project (Microsoft Graph, not Google OAuth or ServiceNow tokens). Depends on nothing built in Story 14 -- independently shippable, sequenced second per Olu's own stated order.
- (event) User confirmed the 2-story addition (14: Google Drive, 15: SharePoint), both extending CAP-2, neither needing a new capability -- same pattern as the original ticketing/calendar/CMDB connector stories.
- (event) stories.yaml re-derived and checked: 15 entries, unique prefix-free string ids '1'-'15', no status field -- all pass. Ids 14/15 have no existing stories/ spec file, confirmed collision-free.
- (note) Reconciliation: Story 16 (Cross-cutting connector and ledger-core hardening) was added directly to stories.yaml by a prior session without going through bmad-spec -- pure hardening (4 converged deferred-work.md gaps: _build_source injectivity, credential strip/non-ASCII, get_record fail-open), no new capability, so no CAP entry needed or missing. Noting for the record so a future reader isn't confused why stories.yaml has 16 entries while this memlog's last decision was for story 15. Proceeding with this run's actual new work: CAP-11/CAP-12 for the DR risk classification capability (roadmap.md's first 'Next' item).
- (capability) CAP-11 -- Tiered DR risk classification. intent: Compute each artifact's DR risk level from its config-declared tier, current freshness against that tier's expiry rule, and its confidence -- closing the gap where tier_sla/expiry_rule/verification_method have existed in the LedgerRecord schema since CAP-1 but no story has ever populated them (ledger_core/projection.py's own docstring: 'no tiering data source exists yet'). success: An artifact whose type has a declared tier gets a computed tier_sla, expiry_rule, and risk level (high/medium/low); an artifact with no declared tier resolves to the most conservative reading (risk unknown), never a guess, mirroring AD-10's existing no-signal-escalates discipline; every value is ledger-core-computed, never accepted as connector/caller input, extending AD-5/AD-9's existing discipline. This is what finally makes CAP-10's policy engine 'automatic' branch reachable against real data -- it requires tier_sla_known=true, which is always false today.
- (capability) CAP-12 -- DR test achievement signals. intent: For a DR test result, compute how well the test actually performed against its declared target -- RTO/RPO achieved as a percentage of target, not bare pass/fail -- and whether it ran inside the program's mandated annual testing window, an independent compliance signal regardless of pass/fail. success: A DR test result artifact carrying target and actual recovery-time fields gets computed RTO-achieved and RPO-achieved percentages; a test scheduled outside the config-declared testing window is flagged even if it later passes; both values are ledger-core-computed, never accepted as connector/caller input.
- (constraint) Amend the existing ledger-core-exclusive-computation constraint in place: 'EvidenceBundle.confidence and ActionProposal.policy_decision are ledger-core-exclusive' becomes 'EvidenceBundle.confidence, ActionProposal.policy_decision, and CAP-11/CAP-12's tier_sla/expiry_rule/risk level/RTO-RPO-achieved values are ledger-core-exclusive' -- same discipline, now covering the risk-classification layer too. No connector or caller may supply a tier, risk level, or achieved-percentage directly.
- (direction) New Non-goal: automatic tier discovery -- an artifact's tier is declared in a git-tracked config file (the same declare-in-config/ledger-core-computes-the-derived-value pattern rezops.policy.yaml already established for CAP-10), never inferred from a CMDB field, a connector, or any live system. A config-driven mapping is a deliberate v1 choice, not a placeholder for a future auto-discovery mechanism.
- (decision) Success signal addition: every computed risk level and RTO/RPO-achieved percentage traces to a declared tier and an observed freshness/confidence/test-result value -- never a bare label with no derivation, extending the same graceful-degradation and never-hide-uncertainty discipline already applied to confidence and policy_decision.
- (event) Self-Validate Pass 1 (coherence, rules 1-6+8) on the CAP-11/CAP-12 addition: PASS. Both new capabilities carry intent+success; intents describe WHAT (which values get computed, from what inputs) not HOW (config file format, exact formula deferred to a story/AD companion), consistent with CAP-1's existing level of detail; the amended constraint bends a real decision (no connector/caller may supply tier/risk/achieved-%); 9 non-goals now, still non-empty, the new one rules out auto-discovery specifically; success signal addition is demonstrable (traces to declared tier + observed value); CAP-11/12 are fresh stable IDs continuing from CAP-10; prose density matches the existing kernel's style.
- (event) Self-Validate Pass 2 (preservation) on this update: walked roadmap.md's first 'Next' item claim by claim. tier_sla/expiry-rule population and tier x freshness x confidence risk computation landed in CAP-11; RTO/RPO-achieved-percentage and annual-testing-window compliance landed in CAP-12. The fifth claim (a DR readiness summary query tool) deliberately did NOT get its own capability or kernel line: it is HOW/mechanism (a specific new MCP tool), and the WHAT it serves -- 'let a user ask what is stale/due/needs attention and get an answer on demand' -- is already CAP-4's existing intent, unchanged. The query tool is a story-level detail extending CAP-4, the same way ledger_get_coverage/ledger_list_records already do, not a new capability. No wrapper-only content found; nothing dropped.
- (decision) Story 17 -- Tier assignment & DR risk classification. spec_checkpoint=true, done_checkpoint=true (user-confirmed: new config-file pattern, first thing to ever populate tier_sla, and unblocks CAP-10's dormant 'automatic' policy branch -- same weight as Story 12's Evidence boundary). Covers CAP-11: new git-tracked tier-assignment config, tier_sla/expiry_rule population, risk-level computation (high/medium/low) from tier x freshness x confidence, ledger-core-exclusive.
- (decision) Story 18 -- DR readiness summary query. spec_checkpoint=true, done_checkpoint=false (user-confirmed). Extends CAP-4 (not a new capability): one new read-only MCP tool aggregating risk/RAG status across three axes (application tier, infrastructure, data centres), mirroring ledger_get_briefing's existing shape. Depends on Story 17's computed fields existing.
- (decision) Story 19 -- DR test achievement signals. spec_checkpoint=true, done_checkpoint=false (user-confirmed). Covers CAP-12: RTO/RPO-achieved-percentage computation for DR test result artifacts, and annual-testing-window compliance flagging, both ledger-core-exclusive. Independent of Stories 17/18 -- operates on test-result artifact fields directly.
- (event) User confirmed the 3-story breakdown for CAP-11/CAP-12 (17: tier assignment & risk classification, 18: DR readiness query, 19: DR test achievement signals). Checkpoints: Story 17 both spec+done (new config pattern, unblocks the dormant policy-engine branch); Stories 18/19 spec checkpoint only, no done checkpoint (routine once 17 lands).
- (event) stories.yaml re-derived and checked against stories-schema.md validity rules: 19 entries, unique prefix-free string ids '1'-'19', no status field -- all pass. Ids 17/18/19 (tier assignment & risk classification, DR readiness query, DR test achievement signals) have no existing stories/ spec file, confirmed collision-free.
- (event) bmad-spec run complete on branch evolution: SPEC.md now has CAP-11/CAP-12, an amended constraint (ledger-core-exclusive computation extended to tier_sla/risk/RTO-RPO-achieved), a new non-goal (no automatic tier discovery), and a success-signal addition. stories.yaml has 3 new entries (17, 18, 19). Story 17: spec_checkpoint=true/done_checkpoint=true. Stories 18/19: spec_checkpoint=true/done_checkpoint=false. Self-Validate passes 1 and 2 both PASS.
- (capability) CAP-13 -- Executive dashboard generation. intent: Generate a self-contained, static HTML snapshot of current DR readiness (KPI tiles + RAG status per tier) from real ledger state via a regenerate-on-demand script -- no hosted server, no new computation, a human-facing view of what CAP-4/CAP-11's existing query surface already returns (get_dr_readiness_summary, list_records, get_briefing). success: Running the generation script produces a valid HTML file whose displayed KPI/RAG values exactly match what those functions return at the same point in time -- the same 'must match a live query' success criterion CAP-7 already established for the periodic briefing; any view without a real backing data source is visibly labeled as not-yet-wired, never populated with fabricated numbers presented as real.
- (constraint) A generated dashboard view without a real backing data source (e.g. dependency mapping, an evidence Q&A panel) must be visibly labeled as such, never populated with fabricated numbers presented as real -- extends the same never-hide-uncertainty discipline already governing confidence/risk (AD-5/AD-9) to the presentation layer.
- (direction) New Non-goal: a live, auto-refreshing, or hosted dashboard -- CAP-13 generates a static snapshot only, regenerated on demand by rerunning the script; a persistent server process is a separate, later, deliberate decision (roadmap.md's 'dashboard phase 2'), not a default extrapolation from this capability existing.
- (event) Self-Validate Pass 1 (coherence, rules 1-6+8) on the CAP-13 addition: PASS. Intent+success present; intent describes WHAT (a static HTML snapshot matching live query state) not HOW (script location/template mechanism deferred to the story); the new constraint bends a real decision (unwired views must be honestly labeled, not faked); 10 non-goals now, still non-empty, the new one rules out a live/hosted dashboard specifically; success signal is demonstrable (displayed values must match a live query, mirrors CAP-7's own criterion); CAP-13 is a fresh stable ID continuing from CAP-12; prose density matches the existing kernel's style.
- (event) Self-Validate Pass 2 (preservation) on this update: walked roadmap.md's 'Executive dashboard, phase 1' item and the user's own scoping decision (Home-only wiring, other views visibly labeled not-yet-wired) claim by claim. Both landed: the static/no-server/regenerate-on-demand shape and the match-a-live-query success bar in CAP-13; the honest-labeling requirement for unwired views in the new constraint. The KPI-tile-redesign concern (CEVA tiles don't map onto Rez Ops's real schema) is deliberately NOT captured as spec-kernel content -- it's a HOW/mechanism decision (which specific tiles) that belongs in the story's own Design Notes, not the capability-level WHAT. No wrapper-only content found; nothing dropped.
- (decision) Story 20 -- Executive dashboard generator. spec_checkpoint=true, done_checkpoint=true (user-confirmed: given this project's history with dashboards -- built and rejected twice before this cycle -- review both the plan and the finished result). Covers CAP-13: ops/generate_dashboard.py, wiring the Home view (KPI tiles + RAG status panel) to real ledger state via get_dr_readiness_summary/list_records/get_briefing. KPI tiles redesigned around Rez Ops's own real capabilities (total artifacts tracked, orphan-risk count, unknown-confidence count, pending drafts, tiers at high/medium risk, data-quality issues) rather than re-skinning the prototype's CEVA-specific tiles. Dependency Map/Ask Rez Ops/test-schedule tabs stay in the generated page but visibly labeled as not-yet-wired prototype views, never populated with fabricated numbers.
- (event) User confirmed the single-story scope for CAP-13 (Story 20: Executive dashboard generator) with both spec_checkpoint and done_checkpoint=true.
- (event) stories.yaml re-derived and checked against stories-schema.md validity rules: 20 entries, unique prefix-free string ids '1'-'20', no status field -- all pass. Id 20 (Executive dashboard generator) has no existing stories/ spec file, confirmed collision-free.
- (event) bmad-spec run complete on branch evolution: SPEC.md now has CAP-13, a new constraint (unwired dashboard views must be honestly labeled, never faked), and a new non-goal (no live/hosted dashboard). stories.yaml has 1 new entry (20), spec_checkpoint=true/done_checkpoint=true per user confirmation. Self-Validate passes 1 and 2 both PASS.
- (note) Update run on branch feat/rez-ops-next-generation: distilling AD-16..AD-20 (ARCHITECTURE-SPINE.md updated 2026-09-12, plus in-place amendments to AD-1/AD-7/AD-12/AD-14) into new capabilities. Input is structured and pre-sorted (spine + solution-design.md, both already adopted companions). Two new adopted companions: docs/architecture-next.md (the delta, written for Phase 1 implementers) and docs/provider-adapter-contract.md (AD-17..20 as an adapter author checklist with definition-of-done and verified runtime facts) -- owned by bmad-architecture, not edited here. docs/product-direction.md (the 3.5k-line plan the spine distilled) is NOT listed as a source: most of it is Deferred to later phases, so it is not fully absorbed; the spine frontmatter already cites it. reviews/ folder is process metadata, excluded.
- (decision) Why amended in place: append one sentence naming the September 2026 opportunity -- the same read-only, ledger-owned discipline should run on whichever subscription-backed local agent runtime the operator already has (Claude Code, Codex, Google Antigravity CLI), launched by Rez Ops itself for unattended work, so no single vendor runtime is ever load-bearing for program state.
- (capability) CAP-14 -- Driven-mode agent runtime. intent: Let Rez Ops itself launch a provider official local agent CLI headless (Driven mode) against exactly the MCP server set a human own client loads (Hosted mode), so unattended work reaches Sensors and Ledger through the same tools a human would, from a runtime package (agents/) that is a peer of ops/ and never part of the Ledger. success: rezops agent run --provider NAME completes a simple non-destructive task through Rez Ops MCP tools for the fake adapter and for each subscription-authenticated real provider; a driven session loaded server set equals .mcp.json exactly, derived per run and never hand-maintained; a launch where not every server attaches fails as MCP_ATTACH_FAILED rather than running degraded; ledger_core/ imports, spawns, or awaits nothing in agents/. Implements AD-16.
- (capability) CAP-15 -- Probed provider health and capabilities. intent: Report what each provider runtime can actually do right now -- installed, authenticated, healthy, its capability set (mcp, headless, sessions, structured_output) and auth mode -- by probing the real runtime at call time, rendered for a human by rezops doctor and consumed by the same code path for any run. success: rezops doctor reports every adapter by probe, correctly distinguishing installed, installed-but-unauthenticated, and wrapper-present-binary-missing (this machine Codex install); health is judged from the runtime structured result, never its exit code alone (Antigravity CLI exits 0 on a denied tool call); a failed probe names a specific failure code and leaves the task bound for that provider pending and visible; no config file supplies a capability claim. Implements AD-17.
- (capability) CAP-16 -- Agent session record. intent: Record every driven session, task, tool call, claim, and outcome as an append-only event stream in the agent runtime own store (agent_data/), current state a pure projection, so what an agent did -- and whether a run failed -- is answerable from the store alone without any provider transcript, and a session can be resumed or cancelled by its Rez Ops session_id. success: every driven run leaves a session.started-to-terminal event trail in agent_data/sessions/; a failure is a session.failed event plus a non-zero exit from rezops agent run, never silent and never an _ops.log.md line; the provider native session id is recorded separately as provider_session_id and resume/cancel address the Rez Ops id; no event payload contains a credential value; no transcript exists unless opted in, and then git-ignored and consumed by nothing; ledger-core never reads or writes agent_data/ and agents/ never writes ledger_data/. Implements AD-18 (the Phase 2 start/resume/cancel/timeout durability is this capability, not a new one).
- (capability) CAP-17 -- Environment-attested provenance on every Ledger write. intent: Stamp every record a ledger-core write tool creates -- RawFact ingest, Draft, EvidenceBundle, ActionProposal -- with who wrote it (session_id, provider, model, mode, auth_mode, operator), sourced from ledger-core own process environment and never a tool argument, so a fact or claim relayed by a driven LLM is attributable and never indistinguishable from an observed fact. success: a record written from a driven session carries exactly that session stamp (the contract suite end-to-end check); a caller-supplied provenance value is rejected as a schema violation; a partial or malformed REZOPS_AGENT_* set refuses the write as PROVENANCE_INVALID, never falling open to hosted; a hosted-mode record stamps mode hosted with operator from REZOPS_OPERATOR, or unknown when unset, never a guess; every record written before the stamp existed reads back as provenance null and is never rejected or downgraded; anything the fake adapter authors carries provider fake. Implements AD-19 (ledger-core half is Phase 3).
- (capability) CAP-18 -- Provider adapter contract. intent: Ship four adapters -- fake (in-process, no binary), claude, codex, google -- each driving one vendor official local runtime under one contract, proven by one shared contract suite (tests/providers/) that the fake adapter both passes and fault-injects against, so the fake is the contract executable definition rather than a stub. success: every adapter passes tests/providers/ with no adapter-specific skips; the fake can inject every code in the shared failure enum; the four negative launches fail with the right code -- wrong .mcp.json gives MCP_ATTACH_FAILED, an extra server in the provider global config gives TOOL_SCOPE_VIOLATION, an ambient API key without per-provider opt-in gives AUTH_MODE_MISMATCH, environment forwarding unavailable gives PROVENANCE_UNAVAILABLE; a search for a vendor name outside its adapter package (ledger_core/, connectors/, shared/, agents/core/) returns nothing; no REZOPS_* value appears in any generated file; nothing under .rezops/run/ or agent_data/transcripts/ is tracked by git. Implements AD-20 (method signatures deliberately deferred to the Phase 1 spike; the seven clauses are constraints, below).
- (capability) CAP-8 success amended in place (AD-7 amendment): append -- once Driven mode exists (CAP-14) the scheduled path runs through the agent runtime; ops/run_scheduled_briefing.py hard-coded vendor invocation is a named, grandfathered exception until Phase 1 makes it a thin shim over the claude adapter, and a driven-mode failure surfaces as a session.failed event plus non-zero exit rather than an _ops.log.md line. Intent unchanged.
- (capability) CAP-10 success amended in place (AD-12 amendment): append -- from Phase 3 the decided event also records the SHA-256 of the rezops.policy.yaml and rezops.tiers.yaml it was evaluated against, so which policy version was used is answerable from the log alone.
- (constraint) No provider runtime is ever a system of record: every fact, claim, decision, and action lives in ledger_data/ and is fully reconstructable with every provider gone; ledger_core/ never imports, spawns, or awaits the agent runtime, and the runtime holds no domain logic in either mode (AD-1 amendment, AD-16).
- (constraint) .mcp.json is the single source of truth for the server set in both modes: a driven adapter derives its provider native MCP config from it per run into git-ignored .rezops/run/{session_id}/, launches the provider with the project root as working directory and its home directory untouched, and a provider global MCP config never merges extra servers into a driven session (AD-16).
- (constraint) Provider capabilities are probed at call time, never declared: no config file asserts what a provider can do; a future rezops.providers.yaml holds routing preferences only, never capability claims; health is read from the runtime structured result, never its exit code alone (AD-17).
- (constraint) Two stores, two writers: agents/ writes only agent_data/, ledger-core writes only ledger_data/, and the only thing crossing between them is a provider session calling the same MCP tools a human would -- no new write tool, no side channel; no secret ever lands in agent_data/ (AD-18).
- (constraint) Provenance is never caller-supplied: ledger-core reads it from its own environment; the launcher sets REZOPS_AGENT_SESSION_ID/PROVIDER/MODE/AUTH_MODE and forwards them by name never by value (an adapter that cannot fails the launch as PROVENANCE_UNAVAILABLE rather than writing values into a file); the stamp is required on write, optional on read (AD-19).
- (constraint) Every provider adapter obeys the seven-clause contract in provider-adapter-contract.md (AD-20): it launches the vendor official runtime and never extracts, proxies, stores, or logs its credentials; auth_mode is recorded on every session, SUBSCRIPTION by default and API only by explicit per-provider opt-in (AUTH_MODE_MISMATCH otherwise); vendor CLI names, flags, and config formats appear only inside that adapter package; failures come only from the one closed enum in shared/, never unknown error; provider selection is deterministic and inspectable, never an LLM choice, with requested and executed providers both recorded; a driven session tool scope is exactly the (server, tool) set from .mcp.json, enforced by strict MCP-config mode, denial of the provider built-in shell/file-write/web tools, and a loaded-server-set check before the first task (TOOL_SCOPE_VIOLATION) -- never a skip-all-permissions flag.
- (constraint) Phase ownership is fixed: Phase 1 builds agents/ (core, the four adapters, agent_data/, the rezops CLI) and changes nothing in ledger_core/, connectors/, or shared/ -- it only sets the REZOPS_AGENT_* environment; Phase 3 owns the provenance stamp on all four write tools, the policy-file hash on decided, and the shared failure enum. Adapter build order is fixed by dependency: agents/core + fake + contract suite first, claude second, codex and google after, each against the same suite (AD-19, AD-20 section 6).
- (constraint) Everything a Sensor returns and everything a provider emits is untrusted data, never instructions -- the reason tool scope is enforced and every claim is validated by ledger-core (AD-11/12/19/20).
- (constraint) Local-first constraint amended in place: v1 runs local-first -- no hosted database, container orchestration, or persistent server process; the agent runtime is invoked per run under a wall-clock budget and exits, never a resident daemon; git is the sole persistence layer for ledger_data/ and agent_data/ alike (AD-7 amendment, AD-18).
- (direction) New Non-goal: inverting the usage model -- Hosted mode (the human own MCP client loading .mcp.json) is unchanged by this evolution; interactive Claude Code does not become the claude provider in interactive mode, and a driven session is not privileged over a hosted one.
- (direction) New Non-goal: missions and tasks, provider routing and rezops.providers.yaml, multi-agent review, agent skills, a typed domain model, and a mission scheduler -- each is Deferred in the spine to a named later phase; Phase 1 builds no routing and the caller names the provider.
- (direction) New Non-goal: a resident agent daemon or polling scheduler -- the OS scheduler invoking rezops agent run is the whole scheduler until a concrete requirement forces more.
- (direction) New Non-goal: fixing AgentProvider method signatures before the Phase 1 spike has inspected the real runtimes -- the contract invariants are fixed now, the interface is designed after inspection, never prematurely.
- (direction) New Non-goal: Rez Ops handling any provider credentials -- extracting, proxying, converting, storing, or logging them, scraping a private web session, or bypassing a vendor rate or usage limit; API auth mode stays unbuilt until a user opts in for a specific provider.
- (direction) New Non-goal: an LLM-free Sensor-to-Ledger relay -- today every RawFact reaches the Ledger through the runtime; AD-19 makes that relay attributable, and removing the LLM from the path would amend AD-1 and is its own later decision.
- (direction) New Non-goal: treating a provider transcript as domain truth -- transcripts are opt-in, git-ignored debugging aids consumed by nothing.
- (direction) Non-goal amended in place: Migrating to MCP SDK v2 in v1 becomes -- migrating to MCP SDK v2 before Phase 3: the <2 pin holds through Phases 0-1 because adapters launch CLIs and never import the SDK; revisit before Phase 3, where the 1.x/2.x elicitation API difference AD-13 leans on would first bite.
- (decision) Success signal addition: each of the three real provider runtimes answers what is the current DR readiness through the same Rez Ops MCP tools with no provider-specific domain logic, rezops doctor reports every provider honestly by probe, and every Ledger record written from a driven session names the session, provider, and mode that wrote it -- never an unattributed relayed fact.
- (assumption) The plan Phase 1 exit criterion -- rezops doctor reports each provider, rezops agent run --provider fake works, each authenticated real provider performs a simple non-destructive task -- is adopted as CAP-14/CAP-15 demonstrable bar, and the Phase 3 exit (each provider answers the DR-readiness question via the same tools) as CAP-17/CAP-18 bar; the spine names both but the plan (docs/product-direction.md) is where they are stated.
- (assumption) Vendor facts this spec rests on (Claude Code 2.1.269 loads all 7 servers/18 tools headless with --strict-mcp-config; Codex CLI headless via codex exec with TOML MCP config; Google Antigravity CLI agy 1.2.0 is the individual-subscription route since Gemini CLI stopped serving it 2026-06-18) were verified 2026-09-12 and are re-probed at every run per AD-17, never pinned by this spec.
- (question) Where does the shared failure enum live during Phase 1? AD-20 section 4 and the structural seed place it in shared/failures as Phase 3 work, and Phase 1 must not touch shared/ -- yet Phase 1 fake adapter and rezops doctor must already report codes from it (PROVIDER_NOT_INSTALLED, MCP_ATTACH_FAILED, ...), and the structural seed also lists shared failure enum under agents/core/. Does Phase 1 define the agent codes in agents/core and Phase 3 relocate them into shared/ alongside the connector/ledger/policy codes, or is shared/failures the one sanctioned exception to Phase 1 no-touch rule? Needs an answer before Story 21 starts.
- (note) stories.yaml check against the updated spec: all 20 existing story descriptions still match their capabilities. Story 11 dispatch note (invoke via claude -p ...; failures append to _ops.log.md) reflects the pre-amendment AD-7 and is exactly the grandfathered exception CAP-8 amended success now names -- historical dispatch text for a shipped story, not a mismatch. No re-run of Story Breakdown needed for existing entries; new Phase 1 stories are offered below.
- (event) Self-Validate Pass 1 (coherence, rules 1-6+8) on the CAP-14..CAP-18 addition: PASS. All five new capabilities carry intent+success; intents describe WHAT (which mode exists, what is probed, what is recorded, what is stamped, what contract is proven) not HOW -- per-provider config derivation formats, event line grammar, payload schemas, and AgentProvider signatures are all left to the companions or the Phase 1 spike; CLI names (rezops doctor, rezops agent run) and failure codes appear because the plan itself makes them the exit criteria and the contract vocabulary, the same way CAP-13 names its script. Every new constraint rules something out (a Phase 1 story cannot touch ledger_core/, an adapter cannot pass values in a file, a session cannot inherit a global MCP server). 18 non-goals, each closes a specific vacuum. Success-signal addition is demonstrable (the DR-readiness question via the same tools; doctor by probe; provenance stamp on driven writes). CAP-14..18 are fresh stable IDs continuing from CAP-13. CAP-8/CAP-10 amended in place, intents unchanged. Prose density matches the existing kernel; the seven-clause adapter constraint is the densest line in the kernel and stays a single line by citing provider-adapter-contract.md for the checklist form.
- (event) Self-Validate Pass 2 (preservation) on this update: walked AD-16, AD-17, AD-18, AD-19, AD-20 and the AD-1/AD-7/AD-12/AD-14 amendments claim by claim, plus architecture-next.md sections 5-7 and provider-adapter-contract.md section 11. Every load-bearing claim landed in the kernel or an adopted companion: two modes/one surface, .mcp.json as source of truth, project-root cwd, no global-config merge, Ledger never orchestrates (CAP-14 + constraints); probe-not-declare, structured-result-not-exit-code, preferences-only future file, doctor as the same probe, pending-visible (CAP-15 + constraint); agent_data/ sole writer, session_id vs provider_session_id, no secrets, opt-in git-ignored transcripts, resume/cancel by Rez Ops id, session.failed + non-zero exit (CAP-16 + constraints); stamp on all four write tools, env-not-argument, by-name-never-value, PROVENANCE_UNAVAILABLE/INVALID, hosted operator from REZOPS_OPERATOR or unknown, required-on-write/optional-on-read with legacy null, fake stamps loud (CAP-17 + constraint); seven clauses, closed enum, four negative launches, fake as executable definition and fault injector, fixed build order, no vendor name outside its adapter, no REZOPS_* value in generated files (CAP-18 + constraints); never-a-system-of-record (AD-1), per-run process under a wall-clock budget and grandfathered scheduled-briefing script (AD-7, CAP-8), policy/tiers SHA-256 on decided (AD-12, CAP-10), REZOPS_OPERATOR shared with approver (AD-14, CAP-17), untrusted-data rule, Phase 1/3 ownership split. Left in companions by design, not dropped: the exact event-type list and per-event fields, the capability-set field names, the per-runtime flag/config/session matrix (contract section 12), the Phase 3 fake-against-isolated-test-root rule (contract section 9), the mission-approvals-vs-single-approval-mechanism conflict and the unautomated commit-and-push gap (spine Deferred, owned by future ADs). One live-source disagreement surfaced as an open question rather than resolved silently: failure-enum placement in Phase 1 (shared/ per AD-20 section 4 and the phase table vs agents/core per the structural seed). Wrapper-only content correctly dropped: reviewer narrative (which lens found which hole), architecture-next.md before/after ASCII picture, the plan candidate interface type names (AgentProvider, AgentSession, ...) which AD-20 itself declines to fix.
- (decision) Open question resolved by Olu: during Phase 1 the agent failure codes (PROVIDER_NOT_INSTALLED ... PROVENANCE_INVALID) are defined in agents/core, keeping the Phase 1 no-touch rule on shared/ literal; Phase 3 relocates them into shared/failures beside the connector/ledger/policy codes as the one system-wide closed enum AD-20 section 4 names. Consistent with the spine structural seed (which lists the enum under both agents/core and shared/failures [Phase 3]) and architecture-next.md phase table -- no source amendment needed. Phase-ownership constraint amended in place to say so; the open question is retired.
- (decision) Story 21 -- Agent runtime core, fake adapter, and the contract suite. spec_checkpoint=true, done_checkpoint=true (user-confirmed: defines the interface every other adapter is measured against, same weight as Stories 1/12/17). The Phase 1 spike proper: inspect the three real runtimes first, then fix AgentProvider signatures; agents/core (session_id minting, REZOPS_AGENT_* env, AD-18 event emission with versioned payload shapes, agent failure codes per the resolved enum decision), agent_data/ sessions store, the in-process fake adapter with fault injection for every code, tests/providers/ contract suite, the rezops CLI with doctor (probe-only by default) and agent run --provider fake. Covers CAP-14/CAP-15/CAP-16 (partial, fake only) and CAP-18 (fake). Touches nothing in ledger_core/, connectors/, shared/.
- (decision) Story 22 -- Claude Code adapter. spec_checkpoint=true, done_checkpoint=true (user-confirmed: first adapter that reaches a live LLM with MCP tools attached; tool-scope enforcement is security-relevant). Passes .mcp.json via --mcp-config with --strict-mcp-config, denies built-in shell/file-write/web tools through Claude permission settings (never a skip-all flag), verifies the loaded-server set equals .mcp.json before the first task, confirms env forwarding by name to MCP children, probes auth_mode; makes ops/run_scheduled_briefing.py a thin shim over rezops agent run --provider claude, closing the AD-7 grandfathered exception. Covers CAP-14/CAP-15/CAP-18 (claude) and the CAP-8 amendment. Depends on Story 21.
- (decision) Story 23 -- OpenAI Codex CLI adapter. spec_checkpoint=true, done_checkpoint=false (routine once 21/22 establish the pattern, same weight as Stories 18/19). Derives config.toml [mcp_servers.*] from .mcp.json per run, confirms the strict/isolated MCP-config equivalent or fails the launch, proves the probe distinguishes wrapper-present-binary-missing (this machine) from installed, codex exec headless with --json events and codex exec resume. Covers CAP-18 (codex). Depends on Story 21.
- (decision) Story 24 -- Google Antigravity CLI (agy) adapter. spec_checkpoint=true, done_checkpoint=false. Derives .agents/mcp_config.json from .mcp.json per run (stdio shape matches), judges health and tool scope from the JSON envelope and denial notices because a denied tool call exits 0 with status SUCCESS, never relocates ~/.gemini, uses permissions.allow rather than a skip-permissions flag. Covers CAP-18 (google). Depends on Story 21; independent of 22/23.
- (event) User confirmed the 4-story Phase 1 breakdown (21: core + fake + contract suite, 22: claude adapter, 23: codex adapter, 24: google adapter) and scope = Phase 1 only; Phase 2 (session durability) and Phase 3 (provenance stamp, policy hash, shared enum) stories deferred to a later breakdown once the spike has fixed the AgentProvider signatures. Checkpoints: 21 and 22 both spec+done (user-confirmed); 23 and 24 spec only (default for routine adapters after the pattern lands).
- (event) stories.yaml re-derived and checked against stories-schema.md validity rules: 24 entries, unique prefix-free quoted string ids 1-24, no status field, all required fields present -- all pass. Ids 21-24 have no existing stories/ spec file (pinned files end at 20), confirmed collision-free.
- (event) bmad-spec run complete on branch feat/rez-ops-next-generation: SPEC.md now has CAP-14..CAP-18 (driven-mode runtime, probed capabilities, agent session record, environment-attested provenance, adapter contract), CAP-8/CAP-10 amended in place, 9 new constraints (+1 amended), 8 new non-goals (+1 amended), a success-signal addition, 2 new assumptions, and 2 new adopted companions (docs/architecture-next.md, docs/provider-adapter-contract.md). The failure-enum open question was resolved by Olu (agents/core in Phase 1, shared/ in Phase 3) and retired. stories.yaml has 4 new Phase 1 entries (21-24); 21/22 spec+done checkpoints, 23/24 spec only. Self-Validate passes 1 and 2 both PASS.
- (event) Validation (independent, second session, 2026-09-12) of the CAP-14..18 / Stories 21-24 amendment against AD-16..AD-20 and plan Phase 1: PASS with one drift finding. Mechanical: stories.yaml 24 entries, quoted unique prefix-free ids, no status field, 21-24 unpinned (no stories/ files yet); CAP-1..18 unique and sequential; all 4 companions + 2 sources resolve on disk; AD-16..20 each map to exactly one CAP (14..18); Phase 1 exit criteria (rezops doctor per provider, agent run --provider fake, non-destructive task per authenticated real provider) land in CAP-14/15 success; build order 21 -> 22 -> 23/24 matches AD-20 s6; Phase 1 no-touch on ledger_core/connectors/shared honoured by every story; ops/ shim in Story 22 is within Phase 1 scope (ops/ is not protected). FINDING (medium, drift): the seven-clause adapter constraint says failures come 'only from the one closed enum in shared/' while the phase-ownership constraint (Olu's resolution) places the agent codes in agents/core during Phase 1 -- two live kernel lines disagree on the same field. Root cause is upstream: spine AD-20 s4 says 'defined once in shared/' and the seed lists the enum under both agents/core and shared/failures [Phase 3]. Fix: one clause in AD-20 s4 ('housed in agents/core during Phase 1, relocated to shared/ in Phase 3, same closed set') and the same qualifier in the SPEC constraint line; Story 21's dispatch note already says the right thing. No other preservation or coherence gap found.
- (decision) Seven-clause adapter constraint amended in place: 'the one closed enum in shared/' -> 'the one closed enum (housed in agents/core during Phase 1, in shared/ from Phase 3)', matching the phase-ownership constraint and Olu's resolution. Spine AD-20 s4 amended upstream in the same pass so source and spec do not diverge. Validation drift finding closed.
